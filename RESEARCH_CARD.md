# EHR Harness 研究卡 v0.4

更新：2026-09-11。主轴已确认；本版裁定多日期与来源边界，采用 C 已实现的序列化作为首轮联调入口，并明确真实样例选择与查询任务。
总控状态见 [CONTROL_PANEL](CONTROL_PANEL.md)，Proposal 正文见 [paper/PROPOSAL.md](paper/PROPOSAL.md)。

## 三层定位

- **论文方向**：面向真实纵向 EHR 的 Data Agent Harness，目标仍暂定 KDD 2027 Cycle 2；投稿日期、赛道及 MICAD 备选安排另行核对。
- **主贡献方向**：2026-09-11 用户转述师兄确认“事件表示 + 查询 Harness”；执行评测验证方法，效果与创新性仍待研究。
- **候选中心命题**：事件身份、时间角色和来源关系，是 Agent 在真实纵向 EHR 上进行可验证分析的底层数据基座。
- **首轮实验 E1**：重复检验的值—时间—来源匹配。E1 先验证机制，论文范围根据后续结果确定。

## E1 问题

在相同模型、相同合法证据和可比预算下，显式组织文档、时间和来源，能否减少重复检验的值—时间—来源错配，同时保留真正的重复观测？

## 任务协议草案

| 项目 | 定义 |
| --- | --- |
| 输入 | 同一匿名病例中获准使用的检验证据块、文档索引、bbox/evidence id 和合法时间信息 |
| 输出 | 每次观测一行：事件类型、项目、值、单位、事件时间、来源 evidence id、状态 |
| 时间 | 区分采样、报告、记录和文档时间；没有依据时使用 `unknown`，不自动把文档时间当事件时间 |
| 重复 | 多份文档描述同一次事件时关联为一个事件；不同时间的真实复测保留多条观测；无法判断时为 `unresolved` |
| 基线 A | 同一模型直接根据共同证据生成事件表 |
| 候选 B | 先按文档、时间角色和来源组织候选事件，再抽取并关联重复/冲突观测 |
| 固定条件 | 模型、证据、输出 schema、上下文策略、重试上限和预算 |
| 指标 | 值/单位、时间绑定、来源支持、重复保留、错误合并/拆分、漏项、额外生成、unknown 适当性、token/调用/耗时 |

## 合成边界例子

同一项目 X：证据 A 为相对日 0、值 4.2、单位 U；证据 B 为相对日 3、值 5.1、单位 U；证据 C 是明确复述证据 A 的报告副本。正确结果保留两次观测，A 与 C 关联为同一事件，B 保留为新事件。若时间只有文档时间而没有采样/报告证据，时间状态为 `unknown` 或未决状态。

## B/C 共享合成开发口径 v0.1（总控 A 定版）

此节供 B/C 立即开展合成开发；真实数据和模型运行仍依具体授权。现有 `bind_candidates` 是 B 的内部校验草案，适配到共享输出由 B 负责，不将其列表下标直接作为来源标识。

| 项目 | 共同约定 |
| --- | --- |
| 观测单位 | 一条检验观测，包含 `observation_id`、`case_token`、`item`、`value`、`unit`、`time_assertions`、`source_refs`；原始行证据保留，多文档是否同一事件另行判定 |
| 时间断言 | 每个日期各自保存 `role`（sampling/report/record/document/unknown）、相对坐标与 `coordinate_id`、日期来源、角色标签来源以及 binding 状态。一个观测可同时有采样与报告时间 |
| 主评分时间 | 首轮显式检验采样时间；报告/记录/文档时间单独保留。只有报告时间的例子保留采样时间 unknown，不将二者替换；后续任务可以另行指定所需角色 |
| 表头作用域 | 一个明确表格表头日期可以作用于同一病例/文档/页的多行，关联边列出对应 `row_id` 与表格范围。跨表、跨页或续页需要自己的范围证据，否则 unresolved |
| 证据标识 | 保持稳定的日期/标签/行 `evidence_id`，本地映射到匿名文档、页、bbox、来源版本；相对日仅在同一 `coordinate_id` 下比较。数组排序下标不作为跨文件 ID |
| 未决与冲突 | 未识别角色为 unknown；角色或日期到行的归属不清为 unresolved；明确同角色证据相互冲突时并列保留，标记 conflicting。直接复用上游角色不是独立验证 |
| 重复事件 | 仅时间相同、值相同不足以自动认定同一事件。首轮以合成例子中的显式副本关系验证身份逻辑；真实关系不清的事件保留待核对 |
| 评分 | 用独立预期答案对齐观测，分别报告值/单位、角色与时间、来源支持、漏项/多项、误合并/误拆分；匹配策略先用开发例子验证并冻结。引用 ID 存在不等于来源支持答案 |

先用两个检验项目共享“采样 day 0 / 报告 day 1”表头的合成例子联调：两个观测各保留这两种时间，并可共享表头证据。再加入另一表格、缺失角色、同值复测、报告副本、冲突时间和一个刻意写错的结果，测试 B 的输出与 C 的评分。

## A 对 B/C 的边界裁定（2026-09-11）

“角色”指日期代表什么：sampling 是采样，report 是报告，record 是记录，document 是文档生成。一个观测可有多个角色的日期。

1. **跨页表头**：允许有证据的跨页关联。须确认同病例、同报告/表格的续页关系和作用行范围，并保留范围证据；相邻页、列布局相同或只出现重复表头都不足以单独证明属于同一次检验。首轮优先做同页，跨页例子单列覆盖与未决计数，随后用带明确续表证据的合成用例扩展。
2. **只有“日期”标签**：先查看对应列、表格标题、上下文或可核验的模板定义。若角色明确且有来源支持，可赋予角色；仍无依据时保存原日期并标记 role=unknown。不得仅凭靠近检验值把它当采样时间。
3. **一行多个日期**：先按角色区分。采样 day 0、报告 day 1 是两条正常断言，分别保存且可同时明确。只有同一目标角色的归属竞争，才进一步用行列/表头/报告身份及相关证据消歧；唯一支持者可选定，并保留判断依据。仍不确定的关联为 unresolved；明确同角色证据相冲突为 conflicting。候选留在 time_assertions 等证据记录中，不为每个日期制造一条检验事件，也不强行压成一个时间。
4. **evidence**：成功的值—时间绑定应有值/单位证据、日期证据、角色证据及两者关联的范围证据。可以来自同一块或不同块，不能伪造缺失引用。只有值清楚时保留可评分值，时间另记未知/未决；missing role_ref 不代表可以自动给它补一个 ID。

未决是中间状态，Harness 应在预算内尝试补查。评分时，参考答案明确而系统未决，记为未解决/失分；参考本身确实缺失或冲突时，正确报告不确定性单独计分。同时报告明确可答项准确率、回答覆盖率和未决原因，不能靠大量拒答提高表面正确率。

## 当前联调序列化（A 采用 C 开发实现）

为马上联调，B 适配到 `src/evaluation/DESIGN.md` 中已实现的 C 格式；该节解决当前字段决策，不改动已发布评分器：

- 顶层用 `protocol`、`observations`，成本可放 `cost`。首轮兼容标签仍为 `research-card-v0.3/shared-synthetic-v0.1`；研究卡说明升为 v0.4 不表示评分器代码已升级。
- 保留原始证据行：`observation_id/case_token/item/value/unit/time_assertions/source_refs`。副本行仍可追踪，最终去重事件另保留到原始行的映射；不能把去重后少行的表直接交给原始行评分器。
- 时间断言使用 C 已有字段 `role/relative_day/coordinate_id/date_ref/role_ref/scope_ref/binding_status`；值与单位用字符串，首轮相对日用整数，保留原始精度，日内先后未知时报告并列/粒度不足。
- `source_refs` 用行证据 ID；日期、角色、范围 ID 分别在时间断言中。ID 的原文/bbox/版本映射属于运行输入的来源索引，测试金答案不作为输入。
- `unknown`/`unresolved`/`conflicting` 沿用 C 的现有契约，不新增未被 scorer 接受的 `resolved_time` 必填字段。没有采样依据时，按 C 约定保留采样未知/未决断言，其他角色日期照常保留。
- 首轮值精确十进制比较、单位字面一致，不启用隐含单位转换；同时报告 joint、full_record 和身份错误。费用缺失为 null，不能填 0。
- C 当前 `event_groups` 仅用于已决合成身份关系。下一版本增加显式 same/different/unresolved 关系及支持证据，再为正确未决关系计分；当前缺少这一能力须报告，不把遗漏关系视为不同事件。
- 已知严格来源支持集先沿 C 实现联调。C 在开发参考阶段补充人工核对的等价支持集合，来源语义未核验前不得仅凭 ID 存在得分。跨页正例、等价来源、日内精度和显式未决关系需要 C 升级测试后发布新评分器版本。

B 下一次交付必须包含自身流程从合成源材料生成的输出及命令；不能直接复制 C 的 EXPECTED 作为“B 的结果”。C 下一次对该输出实际评分，区分格式不兼容与算法错误，保留现有 release 的版本/哈希。

### 各 session 当前交付

- **B**：按上节适配已实现评分器，交一份自身生成的合成提交；继续完善多角色日期、表头范围和稳定来源，然后接获准真实开发证据。完成标准是提交与可复跑命令，而不只是内部单测。
- **C**：接收 B 的实际提交并生成第一张集成诊断表；按需升级跨页/等价来源/显式未决关系测试。维护开发样例筛选与参考模板，真实内容留在受控环境。
- **A/用户**：A 处理字段分歧；用户对照获准证据核查少量开发事件。开发参考集用于调试；最终独立测试另选未用于调试的病例，冻结答案后再使用。
- **查询 Harness 起步**：按下方 Q1/Q2 实现查询→选择证据→形成事件→检查→返回，再扩展 Q3–Q6；C 用独立预期结果计分。数据整理查询由 A 定义，诊断/用药建议不纳入首轮。

## 真实开发样例与大批数据的使用

以下为分阶段工作范围建议；新增病例正文处理和模型调用仍核查对应具体授权，不要求重跑全量 inventory 或 OCR。

1. **现在就用既有真实开发证据**：在现有两例获准开发病例中，从三份检验文档先选 10–20 条观测，按 C 的 REVIEW_TEMPLATE.md 核对值、时间角色、来源和未决原因。一份没有合格日期候选的文档仍要记录，不能只挑能成功的输入。是否存在真实副本、跨页或多次检验由来源核实，样本没有的情形用合成测试补足，不虚构真实覆盖。
2. **大库先参与选样**：依据既有文件名级清单或匿名元数据选择病例，分开普通样本与困难样本。按页数、检验文档数量、版式、跨页、日期缺失和 OCR 质量分层，层内固定种子抽样；记录选择原因，不按待测模型是否正确挑样本。
3. **下一批扩大**：B 输出与 C 评分接通、10–20 条开发参考可回放后，可提议约 20–50 个获准病例做分层开发小批；起步数量不是论文最终样本量。困难定向集和代表性抽样集分别报告，未决/失败保留在统计中。
4. **正式独立评价**：按患者/病例身份划分，排除开发病例与其副本/近重复；目录数不能直接当独立患者数。同一患者不同就诊需同组或明确时间协议。评价集大小按初试错误率、任务覆盖与标注成本决定；冻结参考与访问范围后再检验定稿方法。
5. **规模曲线**：模型和数据权限落实、失败/重试/费用计数可用后，以递增批次测吞吐、token 与成本、输出覆盖率和错误分布。大批量未标注输出只能支持成本/覆盖等指标，不能直接当作准确率证据。

B/C 都在本地维护代码并可通过 SSH 执行获准服务器任务；C 已完成合成评测代码的远端同步。当前即可利用已授权的小样本和匿名元数据，不必等待整个医院数据治理完成；这与全量向模型发送病历是不同执行范围。

## Harness 起步查询（A 定义）

| ID | 自然语言问题 | 检验的能力 |
| --- | --- | --- |
| Q1 首先实现 | 列出项目 X 的全部检验观测，按明确采样时间排序，附值、单位和来源；时间未明的另列 | 重复观测保留、时间角色、来源支持 |
| Q2 首先实现 | 截至相对日 N，项目 X 最近一次有明确采样时间的结果是什么？ | 指定角色的时间筛选；同日无法分先后时返回并列候选 |
| Q3 | day A–B 之间项目 X 实际测了几次？哪些记录只是同一次检验的副本？ | 事件身份；未决关系存在时报告已确认次数与未决数 |
| Q4 | 找出采样日和报告日不同的检验，并列出两种时间和证据 | 多角色时间断言和角色来源 |
| Q5 | 查询缺少明确采样时间的项目 X；哪些可通过相关表头/续页/重复报告补充确定？ | 定向补查、消歧、覆盖率和调用成本 |
| Q6 | 对项目 X，哪些相同事件的记录存在值、单位或同角色时间冲突？ | 冲突证据定位，保留来源差异 |

以上是数据整理与核验需求，不依赖用户自行设计临床诊断任务。Q2 使用明确的时间角色和证据可见范围，只有日粒度时不声称能判断日内最新。首轮不把同日/同值自动视为同一事件。

## 论文候选机制链

1. **Temporal-Provenance Event Identity Layer**：让事件身份、时间角色、来源、重复和冲突成为可判定对象。
2. **Query-Conditioned Event Materialization**：根据任务和不确定性选择要物化的事件与证据，记录可回放决策。
3. **Execution-Grounded Evaluator**：用事件级、时间级、来源级和成本指标验证前两点。

三点是候选贡献，必须用消融和独立评价证明。增加 Agent 角色、输出 JSON、增加时间列或引用字段本身不算创新。

## 实验阶段

| 阶段 | 目的 | 当前状态 |
| --- | --- | --- |
| E0 | 合成事件、重复、冲突和评分器 | C 自检与远端测试完成，待 B 真实提交联调 |
| E1 | 重复检验的值—时间—来源匹配 | B 同页表头物化合成草案完成，待序列化对接与获准真实开发样例 |
| E2 | 查询驱动事件物化和成本曲线 | 可先设计合成查询，真实对照待 E1 与统一事件接口 |
| E3 | 独立病例主实验与模块消融 | 待参考集和固定预算 |
| E4 | 长度、重复程度、时间歧义和 OCR 质量分层 | 待 E3 |

## 当前状态

- B 最新记录包含同页表头物化原型，35 项合成测试通过；首次提交因使用单一 `event_time` 被安全拒绝，随后统一夹具通过 C 评分，但脚本直接读取 C EXPECTED，因此只是接口烟雾测试，不是 B 算法质量结论。
- C 已完成 12 条合成观测及评分自检，状态记录 Windows/远端各 45 项评测测试通过；这不是 B 结果或模型效果。
- 事件身份完整机制、查询驱动流程、真实开发参考集、独立评价与真实模型 A/B 仍待推进；当前共享序列化已通，最短路径是让 B 从源材料独立生成后再做实际评分。
- 大规模真实肺癌 PDF：用于现实分布和压力测试，不自动等于创新或金标准。

## 可被推翻的结果

- 直接抽取在相同证据和预算下已达到相同质量。
- 完整流程的收益只来自更多调用、更短上下文或更准确的输入时间。
- 事件身份层只改善日志可追溯性，却不改善事件匹配或错误归因。

## 待师兄确认

1. Q1–Q6 的数据整理查询是否覆盖团队近期需要，后续要补充哪类真正临床分析任务？
2. “多时序、模型层、protocol”分别对应这条主线中的哪一层？
3. E1 作为第一轮是否合适，下一阶段优先扩展哪些事件或分析任务？
