# 第一张研究卡 v0.1

状态：任务已由用户确认（2026-09-05）：重复检验与时间匹配；字段与评分细节为 v0.1 设计草案，由 B/C 核对既有接口后交总控定稿。
目的：在下次汇报前完成一次可解释、可重跑的小型对照。

| 项目 | 候选定义 |
| --- | --- |
| 任务 | 从多份检验记录中提取重复事件，将项目、数值、单位、明确时间与来源正确配对 |
| 输入 | 同一匿名病例已获准使用的证据块与文档索引；复用既有 OCR/bbox 和允许使用的时间表达 |
| 输出 | 每次观测一行的事件表：item、value、unit、event_time、evidence_ids；缺失时间保留 unknown |
| A 条件 | 同一模型依据共同证据与固定输出定义直接抽取 |
| B 条件 | 同一模型先按文档及明确时间组织证据，分组抽取后合并重复事件 |
| 主要设计变量 | 证据与时间的组织过程；分组方式和合并规则在运行前写定 |
| 评分 | 原文明示字段/事件匹配、时间绑定、来源支持；同时报告漏提取、额外生成及实际调用成本 |
| 首轮规模 | 10–20 个题目实例用于开发调试；按病例区分开发与后续独立测试，不把少量开发结果当最终论文结论 |
| 本轮产物 | 一页任务定义、一张对照表、2–3 个错误分析、下轮要检验的一个问题 |

## 合成例子：先把任务讲清楚

证据 E1：相对日 0 的某检验项目 X，结果 4.2，单位 U。
证据 E2：相对日 3 的同一项目 X，结果 5.1，单位 U。

期望输出为两条事件：X / 4.2 / U / day 0 / E1；X / 5.1 / U / day 3 / E2。
评价需要区分值提取正确、时间绑定正确、来源支持正确。保留两次观测，明确记录无法确认的时间。

该例纯合成，无医学含义；真实任务字段和单位按既有问题库与源证据定义。

## 实验约定草案

- **时间接口核对（2026-09-05 源码检查）**：现有全量 L2 按单份 PDF 计算相对日期，跨文档的 day 0 不保证同一天。B/C 先验证并补齐病例共同时间或文档偏移元数据；两组使用相同时间表示，缺依据保留 unknown。该适配尚未实现，可复用既有 OCR，不预设重跑。详见 results/2026-09-05_storage_layers_quality_audit.md。
- 两组使用同一模型版本、同一批允许使用证据、同一输出要求与评分规则；明确上下文截断策略。
- 分组可能增加调用：记录实际 token、调用次数和耗时，并按可比预算补充对照，不能将更多计算带来的收益全部归于时间组织。
- 参考答案由人对照允许使用的证据核对。模型可提出候选，但自身输出不直接成为真值。
- 起步只评价明确写出的事实与时间；来源含糊与 OCR 不清单独记录。临床推断需要另行定义评价。
- 本卡尚未指定真实模型或调用授权，合成接口调查可以先行。

## 已确认与下一步

- 用户已选择重复检验与时间匹配。
- B/C 可先调查接口并用合成例子联调；字段定义、评分和首个真实实验配置由总控统一定版本。
- 本轮尚未启动真实模型实验；下一步用户先完成 DataSpace 阅读卡，B/C 完成接口与评分建议。
