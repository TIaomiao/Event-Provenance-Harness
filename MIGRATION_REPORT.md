# 研究资料归集与远端文件统计

日期：2026-09-05。用户确认本地 PC 为论文唯一主工作区。

## 已执行的归集

- 将原首轮研究目录的全部普通文件复制到 archive/context-v001/，保留原结构。
- 将两份既有研究规划复制到 archive/planning/；选定两份历史文献笔记放入 references/。
- 研究卡、DataSpace 阅读卡和 DSH 讨论文件复制到当前根目录。
- 重建本地 A/B/C 分工、上下文与状态入口；后续代码在本地维护，获准运行通过 SSH 在 Larry 执行。
- 每份原始资料的复制路径和 SHA-256 记入 migration_manifest.json，并逐份校验。原文件保留；旧研究入口将指向本目录。
- 共 15 份原始资料归集校验通过；Git 对历史快照和导入文献保留原始字节，避免换行转换破坏哈希。原文中的 Markdown 行尾换行标记按原样保留。
- Larry 原始数据、OCR 输出、身份映射、环境依赖和工程源码未迁移。现有工程继续承担数据处理，研究代码在需要时复用其接口。

## 文件数量：只读目录遍历

统计口径：普通文件与符号链接分开计数，不跟随符号链接，不读取文件正文。统计期间目录可能有少量变化，不是原子快照。

| 范围 | 普通文件 | 子目录 | 符号链接 |
| --- | ---: | ---: | ---: |
| Larry 的 EHR pipeline | 1,431,932 | 387,070 | 340,975 |
| zian 的 internship | 39 | 23 | 0 |

Larry 文件主要组成：

- pilot_output：1,416,819 个普通文件，约占总数 98.94%。
- 两套 Python 环境合计：10,530 个文件。
- private_manifests：3,844 个文件。
- 项目根目录：173 个普通文件，其中 135 个 Python 文件、18 个 Markdown 文件。

这些数字是文件系统条目数，不能解释为患者数或治理完成数。EHR pipeline 外部的原始 PDF 数据目录未纳入本次统计。

zian 的预期 EHR 对应目录不存在。在 projects、internship、archive、worktrees、scratch、notes 内做至多六层的目录名搜索，跳过环境、隐藏目录及数据/私有目录，未找到名称含 EHR 的目录；未据此声称整个账号绝无相关文件。zian 的 internship 是会议与同步工作区，其 39 个文件的统计不代表账号总文件数。

本轮没有移动或删除患者数据，没有启动 OCR 或模型实验，也没有修改运行服务。
