# 数据外发授权记录（2026-09-14）

用途：模型筛选批量测试。

## 授权内容

- 授权人：w（项目研究者本人）。
- 时间：2026-09-14。
- 范围：**先跑 20 例**；拟从肺癌 EHR 数据治理控制台已复核的 200 例池中选取。
- 数据形态：经治理流程处理、已做一定脱敏的 OCR 文本。
- 接收方：`https://<GATEWAY_HOST>`（第三方 LLM 中继网关，经 CNAME 解析至 `<GATEWAY_IP_HOST>`，境外 IP）。

## 执行约束（由 DSH 执行方自行加设，除非授权人另行指示）

1. 脚本在 **Larry 上运行**，OCR 文本不下载到本地；只有 prompt 离开受控主机。
2. 不整目录外发；按文档逐份、最小必要范围发送。
3. 逐次记录 `usage` 原始块到 `.local/cost_ledger.jsonl`（成本与"实际服务模型"的双重证据）。
4. 不外发 PDF 图像、身份映射、参考答案或任何非检验内容。
5. 结果只保留聚合指标；模型原始响应不进入仓库。

## 执行方保留的意见（已向授权人说明，授权人知情后仍授权）

- 该网关为多级中继，上游为第三方账号池；无法从网关侧证明实际服务的模型身份。
- 医院原始记录的第三方跨境传输通常需机构层面审批，研究者个人授权可能不足以覆盖。
- 建议：本次仅用于**开发阶段的模型筛选**；论文最终结果应尽量走可追溯的官方通道，或在论文中如实声明并附 usage 指纹。

## 状态

- [ ] 待确认最终样本来源（200 例池中是否含检验报告类文档）
- [ ] 待确认测试目标（已建议由"抽表准确率"改为"采样事件计数与依据声明"）
