# EHR Harness 论文工作区

## 适用范围与称呼

本目录 `G:/Intern/ehr_harness_research` 是用户确认的论文唯一主工作区，适用于本地 Codex 与 Windows DSH。父级 `G:/Intern/AGENTS.md` 及目标目录中适用的安全、数据和工程规则继续生效；不得绕过。存在实质冲突时，说明具体冲突，不自行扩大权限。

在本研究项目中称呼用户为“w”；其他项目不自动沿用。帮助大二、首次写论文的用户理解研究问题、证据和取舍，使用简体中文与具体例子。

## 研究目标与推进原则

**当前研究中心是“可复用的医疗数据准备 Harness + Agent 整体方案”，这是用户已选定的工作方向，不再作为待二选一的问题。**

- **AI-ready** 对应使用目标与研究价值：使真实医院病历更便于后续 AI 系统可靠使用，并检验任务效果、重复准备负担与资源代价。
- **Harness + Agent** 对应实现框架：将清洗、结构整理、归一化、来源核验、时间关联等能力组织为可调用组件，由 Agent 编排执行。必须区分已有实现、待接入能力和设计提案，不能以架构图代替实现证据。
- 整体方案的任务表现与可复用性是主要研究对象；已有多层处理属于可复用实现和候选消融。层数、旧五指标与旧 benchmark 均不自动成为当前协议，不预设逐层涨分或结构化必然更好。
- 当前优先形成经原文核验的相关工作、具体任务及输入输出、合理对照、判定依据和可评估的系统设计。具体工作单元只由 `CONTROL_PANEL.md` 维护；不从历史待办自动恢复实验。
- 任务、对照、判定规则与资源边界尚未明确时，不启动新的真实数据批量实验。文献阅读、只读审计、合成测试及已有结果的离线核对可以按当前任务继续，不必反复请求许可。
- 不锁定未经确认的投稿会场、截止时间或创新结论。新增任务建议应给出具体推荐及理由，不把能自行核实的事项全部变成用户选择题。

## 活动文档与读取顺序

日常全局入口收敛为五份；源码旁必要的接口、测试和运行说明不受此数量限制。

| 文件 | 唯一职责 |
| --- | --- |
| `README.md` | 项目介绍与导航，不重复维护进度、分数和协议 |
| `AGENTS.md` | 工作规则、读取顺序、协作与安全约束 |
| `CONTROL_PANEL.md` | 唯一当前状态：工作单元、主要写入会话、版本、完成证据、阻塞和下一步 |
| `paper/PROPOSAL.md` | 唯一现役研究规格：研究问题、整体架构、任务、对照、指标、验证设计与边界 |
| `related_work/AI_READY_EVIDENCE_MATRIX.md` | 唯一活动文献台账：原文定位、核验状态、结论、局限、用户阅读反馈与设计影响 |

每个新会话先读适用规则和 `CONTROL_PANEL.md`，再按任务读取 Proposal、文献台账及相关代码。`results/` 保存证据，`references/` 按需读取，`archive/` 仅供追溯；不默认递归载入全部材料或整个旧 session log。

`paper/AI_READY_PLAN.md`、`RESEARCH_CARD.md`、`discussion_notes.md`、旧角色状态和旧周报协议不再发布当前全局指令。整理时提取仍有效的技术事实并归档原件；路径尚未迁移不代表其旧待办仍有效。适用的子目录规则仍需读取，发现其引用旧安排时纳入整理，不直接忽略安全约束。

不要新增平行 Proposal、全局计划、角色总控或每次工作单元的交接 Markdown。必要的新技术文档先判断能否写入上述入口或现有模块说明；迁移与审计所需的清单可保留。

## 会话与协作

- 一个推进会话负责一个有明确验收条件的工作单元。目标或阶段改变、反复出现旧结论污染时，在安全检查点交接并新开；不机械按轮数或压缩次数切换。
- 同一工作区同一时段默认只有一个主要写入会话，由 `CONTROL_PANEL.md` 标明。阅读与审查会话默认只读；另开聊天窗口不视为代码隔离。不再默认沿用固定 A/B/C 分工或等待旧角色批准。
- 子任务必须有明确范围和产物。需要并行写入时，先明确隔离方式及文件归属，由主要写入会话统一整合；不得同时修改共享入口、混合暂存或覆盖他人改动。
- 阅读会话交付“原文定位—核实结论—设计影响—局限—用户理解或疑问”，由主要写入会话同步到文献台账。区分 AI 核验状态与用户亲自阅读进度；不以模型读完代替用户读懂。
- 交接只记录分支与 HEAD、重要未提交改动、已完成证据、未完成事项、活跃后台任务及唯一下一步。当前快照写入 `CONTROL_PANEL.md`，完整聊天保留为历史；不复制整段历史进新会话。
- 新会话必须用文件和执行证据核对交接。未知进程状态明确标注，不能假定旧会话、子任务或服务器任务已停止；不为交接擅自杀进程、重置仓库或重启服务。

## 文献与实验依据

- 文献结论核对到原文的章节、表格或结果文件，标清检索线索、摘要、原文部分及方法结果的核验程度。没有读到的内容不补写为事实；搜索摘要或多个 AI 一致不等于已验证。
- 分开标注会议明确要求、用户决定、整理建议、DSH 提案与实验观察。不能把建议升级为师兄批准，也不能把候选主张写成已有发现。
- 对照必须明确输入、工具、任务与预算。原文不能只因单次输入超限就被判为不可用；整体方案比较与组件归因分开，预计算、索引和工具差异如实记录。
- 参考答案需独立核对，开发样例与留出验证分开。准备流程不得读取留出测题定制答案；机械问题与需要临床解释的问题分开，涉及未定义的临床含义时提出具体疑点。
- 任务正确性、证据支持与流程完成性分别核验；格式可解析或引用可定位不等于内容正确。“没有”“不知道”“未运行”不得混用。失败、额外生成、时间关联错误和重试都要报告。
- 完整记录准备、索引、查询、修复／重试及人工时间；不只记录最有利的一段成本。允许无收益、负结果或证据不足；未测出差异不等于已证明等价。
- 论文消费者与开发用模型分别配置。记录可获取的请求别名、实际返回型号、提供方、版本和运行配置；无法确认的身份标未知，不凭命名或同一家族多模型宣称普适。

## 数据与服务器执行

- 用户已明确取得数据使用许可，按已获授权范围继续，不因换会话重复索取同一授权。许可不自动扩展到新增外发、公开发布、生产修改或超出既有预算的支出；确需扩权时只确认具体新增事项。
- 本地保存研究资料、开发代码、合成样例、配置和允许回传的匿名聚合结果。原始 PDF、OCR／病历正文、身份映射及含患者内容的参考答案留在 Larry 受控环境；不整目录同步运行产物、虚拟环境或私有状态。
- 可通过 SSH 执行获准命令，不依赖服务器上的特定编程会话。运行前读取目标目录规则，将明确版本发布到独立运行目录，记录代码版本、配置、数据范围及产物位置。
- 复用已有 EHR 数据与处理基础，不把本地研究任务扩展为全量 OCR、数据外发、服务重启或生产修改。实验模型供应商、输入范围与费用遵守实际许可。
- API 密钥、凭据和私有日志不入库。公开仓库中的文件即使不在 Pages 白名单内，也不能当作私密资料。

## 工作单元、验证与版本

每个工作单元先核对分支、HEAD、未提交／未跟踪文件及其他写入任务。结束后只更新对应研究内容、必要证据和 `CONTROL_PANEL.md` 的当前快照，不再维护多份角色状态。

当前快照保持简短，包含：当前工作单元、主要写入会话、代码／协议版本、实际完成及验证依据、阻塞与未知、唯一下一步。阅读纠错、设计澄清、整理和合成测试都是有效进展；不以调用次数、新分数或新实验作为每轮汇报要求。

**用户 2026-09-17 的自动提交与推送授权继续生效，不必每个工作单元询问：**

1. 提交前执行仓库实际敏感内容检查与 Pages 守卫规则，包括适用的 `.local/sanitize.py`。检查缺失、失败或无法确认时，不声称通过，不绕过检查进行公开提交／推送。
2. 只暂存本工作单元的明确路径，检查暂存差异；不使用无差别整库暂存，不纳入他人改动、凭据或受控数据。只读且无变更的工作单元不制造空提交。
3. 核对当前分支及远端。在确认本单元位于 `main` 后自动提交并 `push origin main`；其他分支按已有合并安排处理，不悄悄切换、覆盖或强推。禁止为整理使用 `reset --hard` 或清除未跟踪文件。
4. 普通连接失败可按既有方式尝试 `git -c http.proxy= -c https.proxy= push origin main`；认证、权限或安全检查失败不得用此规避，也不无限重试。
5. 推送后核对本次提交 SHA 对应的 GitHub Actions 结果。触发 Pages 发布时，确认部署结论并核对实际页面／链接；尚未完成写“待验证”，失败写明原因。Git 推送成功不等于部署成功，HTTP 200 不等于内容已更新。

## Dashboard 与历史管理

- `dashboard.html` 是活动文档的展示入口，不是第二份协议。当前页面展示研究中心、阶段、阅读反馈、系统与任务设计、可追溯证据及下一步；历史曲线和旧 benchmark 通过独立历史入口访问，不藏在当前页面的隐藏区块中。
- 状态、数字和阅读进度必须来自上述活动文档及对应证据。优先通过仓库实际构建工具生成页面；若尚无工具，先核实并建立轻量生成方式，不虚构脚本或自动同步能力。生成后同时检查本地 `file://` 打开与 Pages 相对链接。
- 文件迁移必须同步修复导航、构建和 Pages 白名单；不能删除缺文件检查来掩盖迁移问题，也不能为通过检查把本地私有材料加入发布。
- 现役正文只写当前有效内容和必要原因，不用更正横幅叠加冲突旧协议。标题、文件名、注释和提交信息描述实际目标或行为，不以被否掉的方案命名。
- 被取代的计划与汇报保留原件后归档，索引记录来源、用途及必要版本信息；已有代码、测试与原始结果不因方向变化而删除或覆盖。引用核验、结果纠错、安全、合规和兼容性事实必须保留，可在审计或归档中追溯，不能以“只留最终产物”为由抹去。
- 大规模整理先备份未提交、未跟踪及本地独有资料，受控备份放在仓库外；保留迁移清单并抽样验证恢复。不把整包备份推到公开仓库。

工具接线与可核实的工程操作由执行端完成；向用户解释关键比较、代表性错误与结论边界。未知信息明确标注，不把计划、文件存在、自动总结或聊天承诺当作完成证据。