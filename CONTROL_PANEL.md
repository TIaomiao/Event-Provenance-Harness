# EHR Harness 总控面板

更新时间：2026-09-19（Asia/Shanghai）。维护人：本地 Proposal 总控对话 A。

## ⚠️ 2026-09-19 会后基线（**先读这一节，它覆盖下面与此冲突的表述**）

师兄 2026-09-19 15:21–15:55 的会议给的是**方向确认 + 优先级重排**，详细依据见仓库根目录的会议基线文件（`AI_READY_EHR_MEETING_BASELINE_2026-09-19.md`）。

**继续的**：研究围绕医疗场景的 AI-ready（病历怎么准备才让现成 AI/Agent 能用）；概念上用 Harness + Agent 组织（AI-ready 对应问题与动机，Harness+Agent 对应实现框架）。

**当前最紧要的不是**加层数、扩病例量、调分数，而是：**调研 AI-ready 相关工作 → 明确 AI-ready 与常规方案的本质区别 → 把预期价值转成可验证的指标和主表 → 再做针对性实验。**

**被降级的表述（不得再当既定前提）**：

| 曾经的写法 | 会后口径 |
| --- | --- |
| "六层已经定稿 / 六层效果已获认可" | 多层处理思路保留，**层数不是论文核心卖点**；六层曲线属**既有探索性结果**，各层地位待新问题定义确定 |
| "旧 benchmark 支持 AI-ready" | 旧抽取 benchmark 归为**前期预实验**，不默认占 AI-ready 主结果位置 |
| "motivation 已对齐 / innovation 已成立 / protocol 已完备" | 会议未完成创新确认、指标定稿、答案依据设计与会场锁定，不得作为执行前提 |
| 英文长标题 | 保留为**工作题目**，无证据表明会议批准其最终形式 |

**三个交付物（会议 §6，本仓已建初版）**：

| 工作包 | 文件 | 状态 |
| --- | --- | --- |
| A 相关工作对照 | [related_work/AI_READY_EVIDENCE_MATRIX.md](related_work/AI_READY_EVIDENCE_MATRIX.md) | v0.1 已建（16 篇 × 六问）；**多数条目只到摘要级，未通读不算完成** |
| B 一页研究逻辑 | [paper/AI_READY_CLAIM_MAP.md](paper/AI_READY_CLAIM_MAP.md) | v0.1 已建（含七个未解问题与五条待检验主张） |
| C 主表设计草案 | [paper/BENCHMARK_DESIGN_V2_DRAFT.md](paper/BENCHMARK_DESIGN_V2_DRAFT.md) | v0.1 已建（空表 + 填写规则 + 对照候选 + 公平性清单） |

**投稿与工具**：CVPR 为近期候选、其他会议期刊保留，**未锁定**；开发以 DeepSeek 为主、降低单点依赖（工具偏好不是论文证据）；CMR 工作站与论文分轨，不急。

**下一次要带的材料**：精选 AI-ready 论文 + 对照表；一页研究逻辑；主表空表及指标定义。

所有 Codex 对话和 Windows DSH 先读本文件。完成工作单元后更新自己负责的行和“最新事件”；详细证据放对应文件。本项目共 4 个逻辑 session，A 同时就是 Proposal 对话，不再另设第五个总控。

可视化入口：[dashboard.html](dashboard.html)；在线入口（启用 GitHub Pages 后）：`https://tiaomiao.github.io/Event-Provenance-Harness/`。GitHub 仓库边界与后续发布节奏见 [GITHUB_REPO_PLAN.md](GITHUB_REPO_PLAN.md)。

## 2026-09-19 六版本实测：哪些准备动作值钱、各值多少

批次 `batch-full-01`（984 病例 / 5,255 文档）；病例级 12 例 × 11 题 × 9 条件 × **3 次重复**（324 次调用）；消费者 `[j]gemini-3-flash`，一次调用、无例子、无重试；建库**零模型参与**。完整结果见 [results/2026-09-19_ladder.md](results/2026-09-19_ladder.md)。

| 条件 | 做对率 | 重复跨度 | 单例 token |
| --- | --- | --- | --- |
| v1 生数据 | **61.6%** | 61.1–61.8% | 72k |
| v2 格式统一 | 62.1% | 61.8–62.6% | 105k |
| v3 单位归一 | 62.8% | 61.8–64.1% | 120k |
| v4 溯源与状态 | 61.6% | 61.1–61.8% | 125k |
| **v5 机器说明书** | **73.0%** | 72.5–73.3% | 126k |
| **v6 纵向时间轴** | **81.9%** | 80.2–84.0% | 130k |
| 反向 v5−溯源状态 | 76.1% | 75.6–77.1% | 120k |
| 反向 v5−单位归一 | 66.2% | 65.6–67.2% | 106k |
| 反向 v5−格式统一 | 67.7% | 67.2–68.7% | 94k |

**四条结论**：① 前四层单独看几乎不动分，台阶在**机器说明书（+11 点）**与**纵向时间轴（+8.9 点）**；② **单位归一是地基，但只有配上说明书才发力**——同一份观测表，无说明书时"超上限行数"只对 12/36，有说明书 29/36，v6 33/36；③ 时间层解锁的是时间题（"检验与医嘱同日天数"3/36 → 19/36）；④ **溯源与状态层没有正收益**（从 v5 拿走它反而 +3.1 点；队列级还出现过整段不按接口返回的格式失败）。

**队列级**：200 例最好条件 d6 = **11.8/16**（四次 11/11/12/13），d1 只有 6.0/16；**整批 984 例最好只有 2.5/16**，且 d3（带逐项观测）**超出上下文上限跑不了**（HTTP 400 ×4）。原文的硬上限仍是 10 例。**队列级方差极大（同一条件 0/16~13/16），只能读趋势。**

**答案键**：几何层作主键、两个独立强模型复核——24 个计数键里 20 个被模型确认、**0 个被模型联合推翻**、4 个属单模型噪声（保留几何键）；极端值题按例剔除（Q1 剔 1 例）。日期抽取审计：17 份文档确认率 **88.2%**。

**顺带发现的两类失败**：队列级 d4 有一次整段不按 JSON 接口返回（记作**格式失败**，与答错分开）；d3 在整批规模超上下文（层越厚，能覆盖的病例越少）。

## 研究方向（当前主轴）

**AI-Ready 医疗数据库——真实医院数据要加工到哪一步，现成 AI 才能直接用？**

**归属（勿混）**：**师兄（= 契阔，同一人）**在 2026-09-18 给的是**方向与两条新轴**——16:32/16:39："可以扣住 ai-ready 来做，而且解决真问题，但是查了一下今年都有相关工作""但咱们是真实的扫描 pdf"；17:36–17:37："我们是真实的原生数据 更具有参考价值以及难度""medical 的纵向序列，需要对时间应对""以及后续加点影像"。**主张"哪些准备动作值钱、各值多少"、题目、M1/M2、与 FHIRBench 的分工对照、必引 FHIRBench/HoloBench 都是我方提案**（已转他征询，尚无明确表态）。对外表述不要写成"师兄定的题"。

- **协议入口**：[paper/AI_READY_PLAN.md](paper/AI_READY_PLAN.md)（五版本定义、五指标口径、实验协议、已有证据、边界、纵向时间与影像两条新轴）。论文正文入口：[paper/PROPOSAL.md](paper/PROPOSAL.md)。**接手任何 session 先读这两份。**
- **两条 Motivation**：M1 真实原生数据——FHIRBench 用的是 100 个合成 FHIR bundle（输入已结构化），我们是从真实扫描 PDF 起，中间那一段没人量过；M2 队列级——原文 10 例到上下文上限、12 例直接 HTTP 400，33,456 例规模上物理不可能。
- **与 FHIRBench 的分工**：它比"已结构化的数据怎么序列化"，我们比"扫描件 → 能用的数据"这一段（版面、印章、定性结果、页码污染都在这一段）。
- **必须引用**：FHIRBench（medRxiv 2026-07-14.26358020）、HoloBench（arXiv 2410.11996）。任何"没人做过"的说法都会被这两篇否掉。
- **六个版本（严格超集）**：生数据 → 格式统一 → 单位归一 → 溯源与状态 → 机器说明书 → **纵向时间轴（v6，2026-09-18 定案）**。每个版本原文一行不删、只加一样东西，因此版本之间的差异只能归因于那一层。v6 加的是"时间已经算好"的材料：`date_role`／`day_index`／每条观测的 `date`／按天的 `timeline`／每病例的 `timeline_summary`；反向点 v6−时间轴（按构造 = v5）。
- **五个指标**：做对率 / 费劲程度（token·延迟·成本）/ 错答率（含错否定）/ 该说不知道时 / 医生核对时间（**代理指标**：引用可解析率 + 走到原文的步数）。
- **正向曲线 + 反向拆件**：病例级正向 6 点 + 反向 4 点；队列级正向 6 点 + 反向 5 点。
- **当前批次**：`batch-full-01`（984 病例 / 5,255 文档）。病例级 12 例 × **11 题**（算一下 6 / 查一下 4 / 真没有 1，含时间题 Q7~Q9 与影像题 Q10~Q11）；队列级 **16 题**（A0~A15，含只有 d6 答得出的 A11~A13）。
- **师兄 2026-09-18 追加两条轴（均已定案）**：① **纵向时间** → 第六层 v6（前置是日期抽取校验）；② **影像** → 按数据实际收敛：语料是扫描 PDF，**没有 DICOM、没有影像标注**，所以做"影像报告题 + 含图页指标 + `report_sections` 字段预留"，图像模态不做。
- **autoresearch 接入（已定案）**：形态是"把实验协议做成 agent 能自动跑"，已落地 `driver.py`（一条命令 9 步、可中断续跑）+ 自动出题 + 结束自检 + 只含聚合数字的 `PUBLISH.json`。

## 当前进度与阻塞（2026-09-18 晚）

- **已完成**：五版本超集构建器（跑在整批 `batch-full-01` 上，零模型建库）；病例级 8 个条件 × 12 例 × 6 题 = 96 次调用跑完（消费者 `[j]gemini-3-flash`，一次调用/例、无例子、无重试）；答案键审计（几何层 vs 两个不同家族强模型）。
- **关键自查**：首轮计数题的消费者答案普遍比键高 1~3，先怀疑自己的键；审计结论是**计数键站得住**（CASE-00001 几何 33 = gemini-3.8 33，逐行 32/33；CASE-00086 9=9；CASE-00336 8=8），是消费者多算；同时抓到白细胞最大值键错 2 例（一例是病历正文里引用的外院值，一例是几何层无法归属的裸单元格），按题剔除不硬算。
- **进行中**：用"几何层 + 两个独立模型"把 12 例的键定稿，然后对已跑完的 96 次调用重打分（不需要再调模型）。
- **阻塞**：**Larry SSH 2026-09-18 晚中断**（banner exchange 超时）。键定稿脚本与队列级脚本都在服务器上，链路恢复后继续；后台队列级任务是否跑完需恢复后确认。
- **恢复后的顺序**：① 收键定稿 → 重打分 → 出病例级正向+反向表；② 队列级 11 题 × 5 正向 + 4 反向（200 例与整批 984 例）；③ 日期抽取校验（纵向时间前置）；④ 写 `results/` 结果文件并回填本面板与 dashboard。

## 2026-09-14 重启说明

本面板仍是项目入口，但此前对话中的主轴、题目和实验结论全部按“候选/待验证”处理；旧文件和 Git 历史保留作证据，不作为当前协议。当前优先验证一个可证伪的 KDD Research Track 问题：给定噪声临床 PDF 集合和纵向查询，预算相同的证据组织是否能减少事件的值—时间—来源错配。真实效果尚未建立。

远端仓库已存在并由本地 `main` 跟踪；线上 Pages 是已发布快照，本地未提交改动不会自动出现在线上。真实数据、OCR 正文和人工参考继续留在 Larry 受控环境。

## 2026-09-15 benchmark 阶段进展（7 轮实测，不需要医生的部分已全跑完）

师兄的反馈是"先把任务的输入输出定义下来，再做基础 benchmark；所有 idea 要在固定的输入输出上做才有可比性；benchmark 的产物就是论文最主要的那张表"。

**核查后的第一个结论：输入输出其实早就定义好了，我们论文这边一条都没接上。**

流水线里已有全套病例级试卷 `lung_cancer_ehr_standard_exam`：**49 个标量问题 + 16 张事件表 + 9 种答案状态码** + `clinical_model_io.schema.json`。
金标准已冻结 v0.30（125 条 = 85 标量 + 40 检验事件，5 例），**审核答案尚未回收**。

**7 轮实测结果（成本约 $1.6）：**

| 轮 | 内容 | 关键结果 |
| --- | --- | --- |
| 1 | 机械状态核验（7 病例 / 343 条） | 可机械判的 43 条里确切错误只有 5 条 → **鉴别力不够** |
| 2–3 | 铺到 16 例次 / 784 条 + 事件表评分器 | 值接地 **82%**（逐字口径；数值归一化后 99~100%） |
| 3 | 结构真值 | 值列几何聚类，12 病例 **2518 行**（点值） |
| 4 | 容忍度指标 + `time_refs` | 加之前时间接地**测不出来**，加之后 142/142 且稳定 |
| 5 | 重定义 `source_refs` | 失败，但查明是 `max_tokens=8000` 截断 |
| 6 | **3 病例 × 3 次** | 引用解析 **100%**、行数**零偏差**、时间接地 **100% 零跨度** |
| 7 | 扩到 **4 张事件表** × 2 病例 | 8 次运行引用解析全部 **100%** |

**2026-09-17 主表（12 病例，同批文档同评分器，本轮 $2.1）：**

| 系统 | 引用可解析 | 值接地（逐字 / 归一化） | 时间接地 | 跑通病例 | 行数恢复（只算跑通的） | 成本 |
| --- | --- | --- | --- | --- | --- | --- |
| 规则/几何（无模型） | 100% | 100% / 100% ※ | 99.5% | 12/12 | 100% ※ | $0 |
| 整病例一次问 | 100% | 87% / **99%** | **100%** | 9/12 | 88% | $0.84 |
| 同类文档合并一次问 | 100% | 88% / **100%** | **100%** | 8/12 | 93% | $0.53 |
| 逐文档分开问 | 100% | 86% / **100%** | **100%** | 9/12 | 76% | $0.60 |

※ 规则法的字段抄自它引用的格子，接地为真是构造保证，只能证明度量没坏。

**结论（和上一版相反）**：① 修掉两处度量缺陷后，**三种问法在可验证性上分不出来**——引用 100%、时间 100%、值 99~100%；② **上一版"上下文一变时间接地掉到 58.6%"是假的**，来自我自己的提示词缺陷（没要求逐字抄、没说日期和时刻常分行、没说跨文档不许拼），写清后三种问法全是 100%；③ 唯一稳定的差别是**系统级失败**：大病例单次调用吐不完 JSON 直接解析失败（CASE-100 真值 514 行），**把失败病例算成 0 分则行数恢复 40~48%，只算跑通的则 76~93%，差一倍**，必须单列；④ **"行数恢复"这一列还不能用**：几何真值没校准，CASE-01 出现 122%（模型抽出的行比真值还多）。

**稳定性**：同配置跑 3 次 → 引用与时间零波动；值接地逐字 95/95/84%，**数值归一化后 99~100%**。⇒ 主口径用归一化，逐字那一列当脚注（差异全部是尾零/千分位/前导零，且都在模型自己引用的文档里）。**跑不出来的病例每次不一样**（CASE-04、CASE-09 在两次运行间状态翻转）。

**2026-09-17 师兄给的新指令（可执行）**：**"得构造数据集"**——用现有数据 + agent／deepseek + 一部分人工审核，
自己造 benchmark 数据集；**"数据集的答案不一定特别准，之前的大模型的 benchmark 就是这样做的"**；
再加一个维度：**一次性完成率**（agent 的强项，不需要太多人工干预或重复跑）。

→ 落地协议见 [paper/BENCHMARK_DATASET_PROTOCOL.md](paper/BENCHMARK_DATASET_PROTOCOL.md)。
答案键分三层做：**L1 机械层（格子几何，不来自任何模型）／L2 模型层（用比被测更强的模型，记录版本）／L3 人工层（自己抽查）**。
**循环论证是这个做法唯一的致命坑**：造键的模型必须 ≠ 被测模型，且要留一个没参与造键的模型做留出评测。

**一次性完成率已实测（12 病例，单次调用、无人干预、无重跑；行数需达几何真值 90% 且引用 100% 可解析）**：
整病例 6/12 = **50%**、同类合并 6/12 = **50%**、逐文档 7/12 = **58%**；每病例成本 $0.04~0.07。
未完成分两类：**一个字都没吐出来**（CASE-02/06/100，输出预算）／**吐出来了但行数不够**（CASE-03 三种问法都是 127~128/200）。

**→ 论文 claim 据此定为**："现成单次调用只能一次跑通一半病例；一个 agent harness（拆分＋自验证＋有预算的重试）把一次性完成率从 ~50% 提到 X%，成本只增 Y%。" 有效性有明确度量、有提升空间、有成本轴、不需要医生。

**2026-09-17 试跑结果（60 题 × 三版本 × 一个独立消费者，$8.51）：**

| 条件 | 做对率 | 成本 |
| --- | --- | --- |
| **v1 原文** | **67%**（40/60） | $1.92（6.35M tok） |
| **v5 结构化（替换原文，有损）** | **58%**（35/60） | **$0.30**（0.98M tok） |
| **v5b 超集（原文+结构化+说明书）** | **65%**（39/60） | $2.20（7.32M tok） |

**三条结论**：① **结构化层没带来准确率提升**（v5b 65% ≈ v1 67%）→ 师兄预判的"没差别说明问题设计错了"成立，原因是**题目全是日期/文档计数，而日期在原文里字面就有**；② **有损的结构化层明确更差**（58%，抽取丢记录：CASE-06 抽出 127 / 实际 330）→ **AI-Ready 层必须是附加的，不能替代原文**；③ 目前唯一可测收益是**成本**——v5 单位成本做对题数是 v1 的约 5.5 倍。

**限制**：CASE-10 的 L1 答案键可疑（两个独立条件都答另一个日期）；R1/R2 题面有歧义（消费者可能把四种页脚时间都算成采集时间）。

**下一步（师兄的分支指令）**：题必须改成"只有结构才答得出来"的——值超阈次数、最大/最小值、定性结果计数、跨文档同日对齐；**答案键改由强模型出候选 + 人工核**。

**2026-09-17 难题集（答案键由两个不同家族模型独立产出，一致才进键，不经人工）**

| 题 | 一致率 | 处置 |
| --- | --- | --- |
| 某项目最大测量值（×2） | 11/11 | 进键 |
| 值小于阈值的次数 | 10/11 | 进键 |
| 关键词零命中 | 11/11 | 进键 |
| 检验与医嘱同日天数 | 7/11 | 部分进键 |
| "阴性"格数 | 0/11 | 剔除（定义有歧义） |

冻结 **50** 条、剔除 16 条。消费者（第三个模型）实测：

| 条件 | 做对率 | 输入 tokens |
| --- | --- | --- |
| 原文 | **100%**（50/50） | 986k |
| AI-Ready 库 | 78% | **174k** |
| 库 + 原文 | 84% | 1,157k |

**三条结论**：① **难度不来自"要不要走结构化"，来自"答案定义精不精确"**——最大值题定义精确故原文也 100%（高于日期题的 67%）；② 结构化层仍无准确率增益，超集比只给原文低 16 点（失败集中在提示词更长时模型返回 null）；③ **单病例问题上唯一的收益还是成本，5.7 倍**。

**方向判断**：师兄的例子是**跨病例聚合**（"4 万个病人里……"）。单病例问题强模型读全文就能答；**跨病例才必须靠库**。已试跑但答案键不完整（12 病例只冻结到 11 个），结果不作数，须先补齐键。

**2026-09-18 队列级题：AI-Ready 从"省钱"变成"能不能做"**

先更正一个数：**OCR 真实进度**（此前只报了 200 例试验批次，漏了全量批次）。34 个全量批次（`batch-full-01`~`34`，每组 984 例）：
**33,456 病例 / 410,467 文档 / 2,130,171 页，已处理 372,775 = 90.8%**（未处理 37,610、失败 82）。
治理流水线**早已实现印章处理**（`SCS-EHR-Governance/config/mvp.yaml`：`seal.strategy: bjca_annotation_full_rewrite`）。

队列级题（答案机械可算、零模型出键）实测：

| 条件 | 病例数 | 体积 | token | 结果 |
| --- | --- | --- | --- | --- |
| 原文 | 8 | 91 万字符 | 79 万 | 6/6 |
| 原文 | 10 | 104 万字符 | 90 万 | 5/6 |
| **原文** | **12** | **128 万字符** | — | ❌ **超 1,048,576 token 上限，跑不了** |
| 结构化 | 12 | 7.5 千字符 | 2,974 | 6/6 |
| 结构化 | 200 | 12 万字符 | 4.8 万 | 6/6 |
| **结构化** | **984（整批）** | **59 万字符** | 23.8 万 | **4/6** |

**结论**：① **原文的硬上限是 10 个病例**，12 个就超上下文——不是慢不是贵，是跑不了；② 结构化容量约 **100 倍**（一次装下整批 984 例）；③ 单病例上 AI-Ready 只是省钱，**队列级上是"能不能做"**；④ 33,456 例的原文约 **6.4 GB ≈ 21 亿 token**，物理上不可能；⑤ 规模大时精度轻微下降（984 例 Q5 差 1、Q6 差 2），基准需把"精确命中"与"容差内正确"分开报。

→ `results/2026-09-18_queue_level.md`

**尚未建立（边界）**：内容正确率（需医生）；16 张表只测了 1 张；规模只到 12 病例；几何真值未校准；只测一个模型；**只有一个人核答案键，报不出真正的标注者间一致性**；无医生则"临床等价匹配 CEM"这一层做不了。

**口径**：引用可解析率作第一道闸；值接地以数值归一化为主口径、逐字口径作脚注；时间按 `time_refs` 判定。

**文件**：[results/2026-09-17_main_table.md](results/2026-09-17_main_table.md)（主表 + 稳定性 + 真值定义）、[paper/BENCHMARK_DESIGN_2026-09-15.md](paper/BENCHMARK_DESIGN_2026-09-15.md)、`results/2026-09-15_*.md`（9 份，其中 82%/13% 两处已被本日结论覆盖）。

**由此得出的 6 条契约改动（全部有实测依据）：**

| 改动 | 状态 |
| --- | --- |
| **`event_tables.time_refs`** | ✅ 已验证：9 次 100%、零跨度 |
| `source_refs` = 观测所在行的证据集合 | ✅ 基本可用；需明确"必须含项目名行" |
| **引用可解析率作第一道闸** | ❗ 截断时全部指标失效 |
| `scalar_answers.confidence` | ❗ 否则 AUC 画不出来 |
| 单位比对归一化、`max_tokens ≥ 16000` | ❗ OCR 输出 `mmo1/L`；8000 会截断 |

**2026-09-17 难题集（答案键由两个不同家族模型独立产出，一致才进键，不经人工）**

| 题 | 一致率 | 处置 |
| --- | --- | --- |
| 某项目最大测量值（×2） | 11/11 | 进键 |
| 值小于阈值的次数 | 10/11 | 进键 |
| 关键词零命中 | 11/11 | 进键 |
| 检验与医嘱同日天数 | 7/11 | 部分进键 |
| "阴性"格数 | 0/11 | 剔除（定义有歧义） |

冻结 **50** 条、剔除 16 条。消费者（第三个模型）实测：

| 条件 | 做对率 | 输入 tokens |
| --- | --- | --- |
| 原文 | **100%**（50/50） | 986k |
| AI-Ready 库 | 78% | **174k** |
| 库 + 原文 | 84% | 1,157k |

**三条结论**：① **难度不来自"要不要走结构化"，来自"答案定义精不精确"**——最大值题定义精确故原文也 100%（高于日期题的 67%）；② 结构化层仍无准确率增益，超集比只给原文低 16 点（失败集中在提示词更长时模型返回 null）；③ **单病例问题上唯一的收益还是成本，5.7 倍**。

**方向判断**：师兄的例子是**跨病例聚合**（"4 万个病人里……"）。单病例问题强模型读全文就能答；**跨病例才必须靠库**。已试跑但答案键不完整（12 病例只冻结到 11 个），结果不作数，须先补齐键。

**2026-09-18 队列级题：AI-Ready 从"省钱"变成"能不能做"**

先更正一个数：**OCR 真实进度**（此前只报了 200 例试验批次，漏了全量批次）。34 个全量批次（`batch-full-01`~`34`，每组 984 例）：
**33,456 病例 / 410,467 文档 / 2,130,171 页，已处理 372,775 = 90.8%**（未处理 37,610、失败 82）。
治理流水线**早已实现印章处理**（`SCS-EHR-Governance/config/mvp.yaml`：`seal.strategy: bjca_annotation_full_rewrite`）。

队列级题（答案机械可算、零模型出键）实测：

| 条件 | 病例数 | 体积 | token | 结果 |
| --- | --- | --- | --- | --- |
| 原文 | 8 | 91 万字符 | 79 万 | 6/6 |
| 原文 | 10 | 104 万字符 | 90 万 | 5/6 |
| **原文** | **12** | **128 万字符** | — | ❌ **超 1,048,576 token 上限，跑不了** |
| 结构化 | 12 | 7.5 千字符 | 2,974 | 6/6 |
| 结构化 | 200 | 12 万字符 | 4.8 万 | 6/6 |
| **结构化** | **984（整批）** | **59 万字符** | 23.8 万 | **4/6** |

**结论**：① **原文的硬上限是 10 个病例**，12 个就超上下文——不是慢不是贵，是跑不了；② 结构化容量约 **100 倍**（一次装下整批 984 例）；③ 单病例上 AI-Ready 只是省钱，**队列级上是"能不能做"**；④ 33,456 例的原文约 **6.4 GB ≈ 21 亿 token**，物理上不可能；⑤ 规模大时精度轻微下降（984 例 Q5 差 1、Q6 差 2），基准需把"精确命中"与"容差内正确"分开报。

→ `results/2026-09-18_queue_level.md`

**尚未建立（边界）**：内容正确率（需医生）；16 张表只测了 4 张（用药/诊断的时间接地是 0/75、1/18，需逐表定义）；规模只到 16 例次。

**文件**：[paper/BENCHMARK_DESIGN_2026-09-15.md](paper/BENCHMARK_DESIGN_2026-09-15.md) + `results/2026-09-15_*.md`（9 份）。

**2026-09-15 工作区清理**：删除 `_lit_tmp/`（131 个文献抓取文件）、`.tmp_audit/`（66 个临时脚本）、5 个空 pytest 缓存目录。**工作区 10.96 MB → 1.51 MB。**

## 2026-09-15 师兄定的工作模式（最高优先级，覆盖此前所有工作方式）

师兄 2026-09-15 早上的原话要点：

- **"idea 这一块儿，你可以多小步快跑吧。就是你可以多跟我更新或者同步，这样我能够把你缺的那部分给你补上。"**
- **"其实像我们来说，我们是有 idea 和这种判断力的。但是呢，我们的缺点就是，没那么多时间去做执行，能力不够。所以，但是你们有执行能力。"**
- 让 AI 出一份**给他看的主要版本**：**不要 Markdown，渲染成 PDF**，他手机上能看。
- **repo 内容要全**；**PDF 和总览面板是最核心看点**；他有追问时靠 AI 对话直接读 repo 回答。

→ **工作方式据此改为：**

| 谁负责 | 内容 |
| --- | --- |
| **师兄** | 出 idea、做判断、补我们缺的那一层（尤其是"哪个临床问题真的重要"） |
| **我们（A/B/C/DSH）** | 快速执行、每天同步、把结果做成他 3 分钟能读完的东西 |

**三条硬规则（取代此前的"自己找方向"）：**

1. **不再自行创建研究方向。** 方向由师兄给；我们只做"把他的话变成可跑的东西 + 把结果如实回报"。
2. **每天一次极短同步**（一句进展 + 一个数字 + 一个卡点），不再攒成大汇报。协议见 [WEEKLY_REPORT_PROTOCOL.md](WEEKLY_REPORT_PROTOCOL.md)。
3. **动手前先花半天测。** 任何"系统会犯错"的判断，必须先在至少一个模型上实测才能写进 Proposal 或面板——2026-09-14 已连续因此错五次。

**面向师兄的交付物**：[paper/BRIEF_FOR_ADVISOR.pdf](paper/BRIEF_FOR_ADVISOR.pdf)（A4 × 5 页，手机可读），HTML 源见 [paper/BRIEF_FOR_ADVISOR.html](paper/BRIEF_FOR_ADVISOR.html)。已从该 PDF 中移除具体患者时间戳，可安全对外。

**2026-09-15 确认的投稿方向**：KDD（必投）+ 今年内另有 CVPR 2027（约 11 月截稿，师兄说"时间特别适合"）、MICCAI（此前的 MICAD 是记错）、ACM MM、以及一个 B 会（师兄口中的"yijieka"，疑似 IJCAI，**待师兄确认名字**）。除 KDD 外具体日期尚未逐条核官方 CFP。

## 当前一句话

目标是 KDD 2027 Cycle 2（约 2027-02，**官方日期未公布**）。**2026-09-14 一天内，本项目连续否证了三版中心命题**：

1. **事件身份 / 副本 vs 复测**：CASE-05/DOC-013 的 11 页检验报告背后确实只有 2 次采血，但 5 个候选模型 × 2 次重复 = **10/10 全部答对**，分组完全正确、都正确引用「采集时间」。→ 正确答案在数据中显式存在时无方法收益。
2. **嵌套 Harness 减少事件数错误**：对抗审稿判定 Harness 为**装饰性**——"把标题与正文的 Harness 换成 pipeline，内容一字不变"。且模型本就不犯错，无收益可测。
3. **发布的实例计数确定性**（多智能体调研 + 对抗审查后的定稿方案）：**已被实测否证**。脱敏发布把绝对日期换成「相对日 + 偏移」，但**时分秒原封不动保留**，同一次采血的 7 份报告相等关系完好 → 计数区间宽度 = 0，K1 kill criterion 触发。

**当前仍未否证的唯一一条**：病程记录的 `[检验组合名][日期]` 引用与检验报告名称**精确命中 0/50**（同一检验两套命名）；**"是否因此导致跨文档重复计数"尚未实测**。

**状态：停止自行生成方向，改为向师兄/科室索取临床问题。** 一页材料见 [paper/BRIEF_FOR_ADVISOR_2026-09-14.md](paper/BRIEF_FOR_ADVISOR_2026-09-14.md)。三版否证的完整记录见 [paper/KDD_IDEA_2026-09-14.md](paper/KDD_IDEA_2026-09-14.md) 与 [results/2026-09-14_model_event_test.md](results/2026-09-14_model_event_test.md)。

**文献调研硬结论**：AI 侧可站位置已基本占满（事件计数 arXiv 2608.31017；检验结果链接 CLinkaRT/TESTLINK 2023；事件时间锚定 2609.13062、2605.15168；OCR 抽取 MedStruct-S 2605.03103、Key Coverage Matters 2605.09440；Harness 效应 DataSpace 2608.03451、Harness-Bench 2605.27922；预算轴 2511.17006；上下文治理 ContextNest 2607.02116、Eywa 2605.30771）。**`AirVx` 经两轮独立检索零命中，确认为不存在的名称，已从全部材料清除。**

## 2026-09-14 采样事件发现（已被上条否决为论文基础，但数据事实成立）

[results/2026-09-14_event_identity_finding.md](results/2026-09-14_event_identity_finding.md)。要点：
- 当前全部结果来自 `pilot_output/non_governance_v0_9_n200_seed20260810/`（面板启动参数 `--run-root`）。引用数据时以此为准。
- 该病例 DOC-013 为 `laboratory_report`，11 页，8 种报告类型，每页页脚含**采集时间 / 接收时间 / 报告时间 / 打印时间**四个带标签的时间字段。
- 11 份报告只对应 **2 次采血**：DAY-A HH:MM:SS–HH:MM:SS（7 份）与 DAY-B HH:MM:SS–HH:MM:SS（4 份）。金标准可由采集时间直接导出，不需人工判断。
- 此前所称的"7/8/10 位临床编码"是**被脱敏涂抹的患者标识**（40 条含 6+ 位数字的记录中 33 条落在 `redaction_hits` 内），不是临床编码。

## 2026-09-14 真伪审计结论（必须先读）

[results/2026-09-14_truth_audit.md](results/2026-09-14_truth_audit.md)。要点：
- `src/` 全量检索模型/网络调用关键词**命中 0 条**——两周工作里没有任何一行代码调用过大模型。
- 此前列入"已完成证据"的 F1 数值（0.6667 / 1.0 / 12-12）是**手写答案对齐手写答案**的自洽检查，`cost.calls = 0`，输入为 `SYNTHETIC-ONLY` 硬编码行。**已从本文件的"已完成证据"中撤下。**
- 服务器侧产物经只读复核**真实存在**（`research_dev_events_v0_1…v0_9`、`replay_metrics`、`review_v0_1/`），26 文档/187 页/13,372 行与声明逐字一致，**没有伪造**。
- 但抽出的内容不可用：v0_1 的 20 条"检验项目"实为 `KPS评分 / 年龄 / 男年龄 / 心率 / P / QRS时限 / R间期`（年龄、生命体征、心电图参数）；v0_8 的"项目名"是文本碎片与日期，且值 20/20 未知、采样时间 20/20 unknown。
- 本地 review 包丢弃了唯一有价值的部分：服务器上 `item/value/unit` 20/20 非空，导出时被压成 `value_present: true` 两个布尔位。
- git 仅 8 个 commit，最后一个为 2026-09-08；09-11 至 09-13 共 52 项改动**未提交**。

## 论文定位（与 `paper/PROPOSAL.md` 对齐）

- **当前题目**：Event-Provenance Harness: Query-Conditioned Structuring for Verifiable Analytics over Real-World Longitudinal EHRs。
- **中心命题**：真实纵向 EHR 的难点不是把 PDF 转成 JSON，而是把分散、重复、冲突且时间含义不同的证据组织成带事件身份和来源的可验证记录。
- **Motivation**：值抽取正确并不代表事件关系正确；重复引用、真实复测、采样/报告/记录/文档时间和来源支持必须被单独表达与评估。
- **候选贡献（均待实验验证）**：类型化多时序/来源事件重建任务；预算受限的查询驱动证据路由与事件物化；可复现的事件级/查询级评测协议。Harness 是实现载体，不能只以“多 Agent”作为贡献。
- **强度边界**：这些仍是候选研究设计；真实模型 A/B、独立评价集、效果和跨任务泛化尚未建立。

## Session 状态

| Session | 入口 | 当前职责 | 状态 | 下一步 |
| --- | --- | --- | --- | --- |
| A 总控 / Proposal | 当前对话，本地 `paper/PROPOSAL.md` | 研究定位、协议版本、跨 session 汇总、师兄汇报 | 进行中 | 维护本面板与 Proposal，决定协议变更 |
| B 原型实现 | 本地 `src/prototype/`，通过 SSH 在 Larry 执行 | 证据适配、事件重建原型 | 已完成跨文档事件分组实测：DOC-013 无行级日期/编号，按页切出 50 事件/473 行；日期层面跨文档命中 8–9/10，名称层面 0 | 核对「组合名=项目名上位分组」；行级事件身份需换取证源 |
| C 评测 | 本地 `src/evaluation/`，通过 SSH 使用受控答案 | 评分器、参考集、错误与成本 | 合成评分器已完成（45/45）；真实参考集未建立 | 冻结核对 schema，准备 baseline/Harness 对照 |
| DSH 文献讨论 | Windows DSH，本地工作区 | 最近邻论文和 KDD 方向审查 | 可按需启用；旧结论仅作候选 | 只比较任务、机制、预算和评价，不扩写口号 |

## 论文阅读队列

| 优先级 | 材料 | 状态 | 下一步产出 |
| --- | --- | --- | --- |
| P0 | DataSpace | 首轮阅读完成，部分设置已复核 | 写出可复用评分和公平对照规则 |
| P0 | HealthFlow | 已有论文与阅读卡 | 提炼计划/执行/评估边界 |
| P0 | ClinLens、HealthAgentBench、EHR-Complex | 已核验摘要/API | 选 2 篇读全文，核对与 E1 的真正重合 |
| P1 | Agentic Context Cracking、Harness-Bench、The Harness Effect | 已核验摘要/API | 比较查询物化、预算和 Harness 效应 |
| P1 | ACIE、ClinTraceBench、TrajOnco、EHR 文档不一致检测 | 已核验摘要/API | 补事件、时间、来源和冲突评测差异 |
| P2 | APOLLO、PAI | 已有本地 PDF | 等主轴确定后按需阅读 |
| 待核 | AirVx、OneEHR、U-EHR | DSH 线索未完成一手核验 | 向师兄索取全名或链接 |

## 实验队列

| 编号 | 实验 | 目的 | 状态 | 依赖 |
| --- | --- | --- | --- | --- |
| E0 | 合成事件与评分器 | 固定值/时间/来源/副本/冲突口径 | 合成验证已完成；不等于真实效果 | 真实参考集 |
| E1 | 查询条件下的事件重建 | 在固定模型/证据/预算下比较 Flat 与约束流程 | 20 条候选已生成，尚未完成人工核对 | 可审计证据卡、开发参考 |
| E2 | 查询驱动事件物化 | 测质量—成本和跨任务复用 | 待定 | E1 机制成立 |
| E3 | 主实验与消融 | 判断收益来自哪个环节 | 待定 | 独立参考集和固定预算 |
| E4 | 分层稳健性 | 长度、重复、时间歧义、OCR 质量 | 待定 | E3 |

## 当前阻塞点

1. **没有任何模型参与**。论文方法是"固定模型、变化证据组织"的对照实验，当前连第一次真实 LLM 调用都未发生；模型供应商、单价、费用授权全部未定。
2. **真实材料到结构化事件的链路实测输出是错的**。检验文档无可复用的结构化 records；OCR 行格式与既有解析器不匹配；13,372 行只得到 4 条候选。**这是整条路线的单点故障。**
3. **没有一行真实参考答案**，没有 Flat / 单层 / 嵌套任何一条 baseline，没有独立测试划分，成本账本全为 0。
4. **KDD 2027 Cycle 2 官方日期未公布**（官网四个赛道 CFP 全部只写 Cycle 1；OpenReview 无 Cycle 2 组）。2026 年先例为 2 月 2 日摘要 / 2 月 9 日全文，仅作预留，不得写入排期承诺。
5. **流水线里已有 45 KB 的 `bbox_table_parser.py`（8 月 14 日）与 `BBOX_TABLE_PARSER_PHASE1.md`**，两周内未被使用，需先做复用评估再决定是否重写。

## 已完成证据（2026-09-14 审计后重列）

**可复用的资产**
- `src/evaluation/scorer.py` + 45 项测试：字段契约明确的观测级评分器（值/单位/时间断言/来源支持/身份关系/成本账本）。**本地实测 45/45 通过**，是两周里最扎实的成果。
- 原型三个模块 + 37 项测试：`temporal_rebase.py`（23 项）、`temporal_roles.py`（12 项）、`laboratory_bbox_adapter.py`（2 项）。本地实测 36 通过、1 项因本机沙箱禁止 unlink 失败（最小脚本可复现同一异常，非代码缺陷）。
- `RESEARCH_CARD.md` v0.4：任务定义、四类时间角色契约、unknown/unresolved/conflicting 语义、表头作用域规则、Q1–Q6 查询清单。
- Larry 受控环境只读访问路径与字段级授权边界（CASE-02 / CASE-05）。
- 文献清单：已逐条核验，约 25 项有可核验 URL。

**文献层必删/必改**
- ❌ 零命中、不得引用：`AirVx`、`MedAILab`、《AI Research Handbook》。
- ⚠️ 缩写错误，须写全名：`ACIE` = *Configurable Clinical Information Extraction with Agentic RAG*（arXiv 2606.19602）；`DIASENTINEL` = `DiaSentinel`。
- ⚠️ 无法核验、不得进入正文的精确数字：DataSpace "410 题 / 88.5% / 15.36 分 / 136 个失败 / 56.6% / 44.1%"、ACIE "7,326 条 / 96.5%"、EHRFlowBench "110 = 55+55"。

**当前证据基线**（2026-09-14 真伪审计后确立）：任何指标的分子分母必须来自真实模型调用或可复现的机械推导。
- 合成自洽分数（手写 JSON 对手写 JSON、`calls = 0`）不作为证据。
- 本地卡内无项目、值、单位、日期、正文的候选，不可核对，不作为证据。
- 服务器上找不到可复现产物的数字（如时间候选适配器的 952），待重跑验证。

## 数据优势与边界

- inventory 记录约 39,560 个病例目录、462,789 份 PDF；这是文件系统规模，不能直接等同独立患者数、事件数或科研可用样本数。
- 可区分度来自原始医院 PDF 的版式/OCR/bbox、跨文档重复与真实复测、时间角色混杂和来源可回溯；不是“数据多”或“用了多 Agent”本身。
- 主要对照数据源：MIMIC/EHRSHOT/UKB 结构化 EHR、MedAgentBench FHIR/虚拟 EHR、DataSpace/FDABench 渲染 benchmark，以及 HealthFlow/ClinLens/ClinTraceBench/EHR-Complex 的纵向 Agent 任务。
- 需要补的可公开统计：独立患者数、文档/事件数、时间跨度、重复率、时间歧义率、OCR 质量分层和可评价病例数。

## 本周最小完成标准

- 用户能用三分钟讲清 Proposal 的一句话主张、E1 任务和一个反驳例子。
- C 有合成评分器草案；B 有日期角色/检验行关联的接口建议。
- 面板更新一次 B/C 状态，保留一项需要师兄决定的问题。

## 论文写作状态

可立即写：Introduction、数据与隐私边界、Event-Provenance 表示、Query-driven Harness、评测协议、数据质量分层、局限性。

暂不定稿：真实 Results 数字、优于 baseline 的结论、Abstract 最终提升、独立测试泛化结论。

## 用户学习动作

- 每篇论文只记输入、输出、机制、评价四项；优先读 DataSpace、HealthFlow、ClinLens/ClinTraceBench 的方法与 evaluator。
- 每读一项机制，写一个纯合成事件例子；再让 Codex/DSH 解释代码如何实现和评分。
- 每周口头复述一次：一句主张、一个反例、一项实验、一个待师兄判断的问题。

## 面板更新规则

- A 更新论文定位、协议版本、实验/阅读队列和师兄决策。
- B 只更新 B 行和 `status/implementation_current.md`（历史写入 `implementation_history.md`）；C 只更新 C 行和 `status/evaluation_current.md`（历史写入 `evaluation_history.md`）；DSH 更新 DSH 行和 `discussion_notes.md`。
- 不把代码完成写成实验完成，不把合成结果写成真实数据结果；新增结论带日期和来源。
- 患者正文、真实参考答案、凭据和原始模型响应不进入面板。
- 面板和 GitHub 规划只展示匿名摘要；远端仓库已存在，后续发布仍须先做脱敏检查，不能把受控数据推送到远端。

