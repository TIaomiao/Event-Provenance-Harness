# 论文阅读与 idea 交接

日期：2026-09-08。用途：另一条对话负责文献、研究主张与写作；当前工程对话负责时间补救、接口和实验执行。

**已确认**：面向计算机会议推进论文；首轮任务是跨文档的重复检验与时间匹配。本地为研究主工作区，Larry 保存受控数据与运行产物。
**建议**：先检验“显式组织文档、事件时间和来源，能否改善重复检验事件的完整绑定”这一机制。
**还没做**：该机制的真实对照、独立测试、文献差异核验以及最终方法定稿；目前没有足以支持论文效果结论的证据。

## 先明确主张可能失败在哪里

- 直接抽取若在独立测试上已足够好，额外组织过程可能没有收益，甚至引入合并错误。
- 表面收益可能来自更多模型调用、更长上下文或更准确的输入时间；实验必须记录并控制这些差别。
- 原文 OCR 不清、不同时间类型混淆、复制报告与真实重复观测混淆，都可能限制表现；需区分输入缺陷、任务定义和方法错误。

因此当前判断是：**方向和首轮实验问题已明确，有候选 idea；最终贡献与有效性仍待研究。** 对“任务已确定”置信度高，对“方法有效或有新颖性”尚无证据。论文推进目标已确定，阅读的作用是把方法、对照与证据补具体。

## 候选 idea 怎样落到可检验的主张

工作主张：在相同源证据、统一时间表示与可比调用预算下，显式的文档/时间组织是否降低重复检验的值—时间—来源错配，并保持事件召回？

| 项目 | 第一轮建议 |
| --- | --- |
| 主任务 | 输出逐次观测事件：项目、值、单位、事件时间、来源；时间无依据则 unknown |
| 基线 | 同一模型直接从给定证据生成事件表 |
| 单一干预 | 按明确文档与时间组织证据，再抽取、合并；分组和合并规则写定 |
| 检验 | 完整事件匹配、时间绑定、漏项/多项、来源支持，以及 token、调用、耗时 |
| 消融 | 逐项撤去组织或核验环节，找出收益来自哪一步；核验可在后续加入 |
| 边界 | 先处理原文明示事实；相同值的两次真实观测应保留，报告副本怎样计数须定义 |

本工程发现的局部相对日问题需要先修好，两组共享相同合法时间信息；该修复属于当前数据的共同前提。多 Agent、harness 是组织形式，具体贡献需要落在明确机制、任务或评价上。

## 我们有什么，还缺什么

下表依据 2026-09-05 的 CONTEXT、RESEARCH_CARD、存储审计；它们是历史状态，本交接未重新核查服务器运行。

工程总控补充（2026-09-08）：已完成时间候选适配器v0.3.1，23项Windows/Linux测试及2例26文档验证，生成952个机械日期坐标候选，检验子集中有161个候选；尚未完成日期角色与事件行绑定。详见ENGINEERING_PLAN.md。该增量属于输入适配，真实模型对照仍未开展。

| 已有基础 | 还需要补齐 |
| --- | --- |
| 大量真实 PDF 的 L1 OCR、bbox，以及 L2 自动脱敏候选产物 | 允许用于实验的小型检验切片、可追溯抽样与当前就绪核查；文件数不能替代病例数 |
| 历史项目有标准试卷、事件表、来源引用及模型请求/响应接口 | 确认可复用部分，接通本轮实验；旧接口存在不代表本轮模型已经跑通 |
| 已确认重复检验任务，已有 A/B 草案 | 明确时间类型、重复观测/副本、unknown 和评分口径 |
| 已发现跨文档局部时间起点不一致 | 小样本验证共同时间适配；保留原产物、记录版本与失败原因 |
| DSH 已通读 DataSpace 并留下详细建议 | 对原文定位核查、最近邻方法比较，以及能转为实现任务的机制说明 |
| 匿名元数据质量抽查 | 独立核对的任务参考答案、按病例划分的开发/测试集、真实对照与错误分析 |

整体 OCR 准确率、脱敏漏检率、本轮事件抽取收益均不能从现有元数据推出。实验参考答案先覆盖可直接核对的事实，临床歧义按需要请专家判断。

## 下一轮阅读只解决四个问题

已有 `discussion_notes.md` 包含 2026-09-05 DSH 的 DataSpace 通读记录，包含评分、合成数据、错误分类和成本提议；原件保留。先继承这份记录，具体数字和结论仍以原论文定位核验。

1. **DataSpace 的哪些设置能直接复用？** 核对输入、模型可见信息、表格/重复行评分与预算设置；记录章节、表或代码位置，并区分原论文事实与我们的改编。
2. **最近邻方法怎样处理跨文档重复事件和时间绑定？** 优先找直接相关的方法与基线，写清它怎样分组、关联、去重、处理 unknown；交付最相关的 2–3 篇对照表，无需先做大而全综述。
3. **我们准备改变哪一个机制？** 用同一个纯合成例子分别演示基线和候选流程；说明模型每一步看到什么、输出什么，以及一项可以推翻主张的结果。
4. **什么评价能说明机制作用？** 找事件级匹配、时间关系、重复项和证据支持的评价口径；确定参考答案来源、病例划分、成本公平比较，以及 OCR 错误如何单独记账。

阅读输出尽量是“一条可核对事实 → 对本实验的一项决定”。可以使用构造保证真值的合成材料验证评分器和边界条件；其结果需要与真实数据结果分别报告。

## 交还工程对话的一页技术任务规格

每次只提交一个版本；未决定的地方填“待定”，工程对话可以先推进不依赖它的接口工作。

```text
版本 / 日期 / 依据论文的具体位置：
要检验的一句话主张：
范围：文档类型、时间类型、重复观测与副本的定义：
输入：证据字段、允许可见的时间、来源引用；样本与数据权限：
输出：事件字段、unknown、重复行；附一组纯合成输入输出：
基线流程：
候选流程：仅改变哪一步，每步的输入输出是什么：
共同条件：模型版本、相同证据、上下文策略、预算与重试：
评分：事件对齐规则、主要指标、漏项/多项/错时间、成本：
验证：开发/测试按病例划分；参考答案核对者；如何避免答案进入模型输入：
可推翻结果：出现什么情况便认为本轮设计没有帮助：
工程验收：命令、输出文件、必要日志、合成边界用例：
待师兄判断的一项取舍：
```

另一条对话将新理解写入讨论记录并给出该规格；当前工程对话汇总版本、实现与验证。正式结果只在实际运行并完成核对后进入论文叙事。
