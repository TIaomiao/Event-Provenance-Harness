# GitHub 仓库规划：EHR Harness 论文项目

更新：2026-09-14

当前事实：远端仓库 `https://github.com/TIaomiao/Event-Provenance-Harness` 已存在，本地 `main` 正跟踪 `origin/main`；GitHub Pages 已启用。本文档下面的边界和发布规则继续有效，但“创建远端”相关旧措辞视为历史记录。

## 建议

继续把现有仓库作为**以论文复现与研究代码为中心的仓库**使用。当前不建议把受控 EHR、原始 PDF、OCR、患者正文、真实参考答案、凭据、服务器运行目录或整份本地工作区直接推上去。

KDD 论文项目通常把“可公开的研究代码、协议、合成数据、评测器、配置和结果摘要”与“受限数据和本地实验状态”分开。论文仓库的目标是让别人能复现方法与评测定义，而不是公开医院数据本身。

## 推荐仓库边界

```text
ehr-harness-paper/
├─ README.md                 # 论文主张、快速开始、复现边界
├─ LICENSE                   # 代码/文档许可，按导师或单位要求确认
├─ CITATION.cff              # 论文稳定后补
├─ paper/                    # 论文正文、补充材料、图表脚本
├─ src/
│  ├─ prototype/             # 事件身份、时间角色、来源接口
│  └─ evaluation/            # evaluator、指标、合成任务
├─ configs/                  # 不含密钥的可复现实验配置
├─ data/README.md            # 数据卡、获取方式、禁止提交真实数据
├─ synthetic/                # 可公开的合成伴随集与扰动生成器
├─ results/                  # 聚合结果、图表输入、运行元数据
└─ .github/                  # CI、issue 模板、贡献说明
```

本地 `archive/`、服务器状态快照、个人学习日志和含路径映射的工作记录不必进入公开仓库；可以保留在本地工作区，或单独放私有归档仓库。

## 建仓顺序

1. 先在本地建立干净提交：删除密钥、患者内容、真实参考答案和大体积运行产物；补 `.gitignore`、数据边界说明和最小 README。
2. 让导师确认仓库归属、可公开代码范围、单位/医院数据政策和论文署名。
3. 创建私有 GitHub 仓库，先推送代码、协议、合成数据和匿名聚合结果；默认不自动公开。
4. 为每个实验打 tag（例如 `e0-scorer-v0.1`、`e1-input-adapter-v0.1`），在 README 记录模型、预算、数据版本和已知限制。
5. 论文投稿或 rebuttal 前，再决定是否公开仓库、释放哪个 commit 和是否提供匿名 artifact 链接。不要把“建仓”与“公开数据”绑定。

## 后续工作规则

- 每次实验先提交协议和配置，再提交代码和聚合结果；不以 notebook 临时输出作为唯一证据。
- 代码完成、合成测试通过、真实数据运行、独立评测完成、论文结论成立分别记录，不能互相替代。
- 公开结果只保留匿名聚合统计、合成样例和错误类别；所有真实病例引用留在受控环境。
- GitHub Issue 用于研究问题和阻塞点，Release 用于可复现版本，Actions 只运行不需要受控数据的检查。
- 任何 push、开源发布或邀请协作者前，先检查敏感内容和单位政策；本项目默认不自动 push。

## 当前状态

仓库决策：**远端已存在，继续使用；发布前仍须逐次做脱敏检查**。当前本地工作树包含未提交的研究状态和新文件，线上 Pages 仍是旧快照；未明确授权前不自动 push。
