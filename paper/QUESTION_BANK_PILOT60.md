# 试跑题库 v1（60 题）：设计、判卷规则、谁来核

师兄的配比是 30 算一下 / 20 查一下 / 10 库里真没有。
我们落成：**每个病例 5 题（3 算一下 + 1 查一下 + 1 真没有）× 12 个病例 = 60 题**。
（算一下占 60%，比他要求的"过半"更高。）

---

## 一、三条硬约束（这是"不用医生"的前提）

| 约束 | 为什么 |
| --- | --- |
| **阈值必须写进问题里** | 实测：检验报告里的参考范围**解析不出来**（12 病例 26060 行检验文本，范围格式命中 0）。所以不能问"有没有超标"，只能问"有没有大于 10"——**阈值由我们给，答案靠数** |
| **不许要临床判断** | 没有医生。任何"这个值对这位病人算不算异常""这个诊断对不对"的题**不出** |
| **答案必须能从文档里数出来或找出来** | 只有这样机器才能独立重算，人工只做抽查 |

## 二、题目来源：只用侦察确认过的字段

| 来源 | 规模（12 病例） | 网格？ | 用来出什么题 |
| --- | --- | --- | --- |
| **order_execution** | 24 份 / 25,366 行 / **8,179 个日期格** | ✅ 9 列 | 算一下的主力：计数、时间跨度、时段分布 |
| **laboratory_report** | 14 份 / 26,060 行 / 18% 数字 | ✅ 9 列 | 算一下：值计数、阈值计数、采集次数 |
| medical_record_front_page | 12 份 / 3,831 行 | ✅ 9 列 | 查一下：住院日期、天数 |
| ecg_report | 11 份 / 5,784 行 / 27% 数字 | 部分 | 查一下：检查次数、日期 |
| progress_note / admission / discharge | 叙述 | ❌ | **只用来出"真没有"型** |

**已被排除的题**（写下来免得以后有人再提）：
- 任何"超出参考范围"的题 → 参考范围解析不了
- 输血相关 → `transfusion_record` 12 个病例里只有 1 份，太稀有
- 病理/影像结论类 → 叙述文档，答案要临床判断

---

## 二之二、一个必须先解决的问题：`order_execution` 的"一条医嘱"定义不稳

实测（12 病例）：用两种定义数"一条医嘱"，结果差 **13%~69%**：

| 定义 | 含义 |
| --- | --- |
| 行带数 | 按纵坐标聚类出来的行 |
| 日期格数 | 带时间戳的格子数 |

| 病例 | 行带数 | 日期格数 | 比值 |
| --- | --- | --- | --- |
| CASE-03 | 363 | 321 | 1.13 |
| CASE-05 | 1208 | 834 | 1.45 |
| CASE-04 | 1200 | 709 | **1.69** |
| CASE-100 | 3273 | 2069 | 1.58 |

比值从 1.13 到 1.69 都有 → **"一条医嘱"现在没有可靠定义**（可能是长医嘱折行、也可能一条医嘱有多个时间戳）。

**处置原则（写进方法论）**：
> **凡是答案依赖"行"这个概念的问题，先确认那张表的行定义稳不稳；不稳就改成"格"级问题。**

- `laboratory_report` 的行定义**已验证**（几何 139 vs 模型 141；CASE-06 330 vs 315）→ 可以出"行"级问题
- `order_execution` 的行定义**未验证** → **只出"格"级问题**：不问"医嘱几条"，改问"涉及多少个不同日期""凌晨时段有多少个时间格"


---

## 三、算一下型（每病例 3 题，共 36 题）

判卷规则统一为：**数值必须完全相等**（归一化后）；同时必须给出出处。
下面"答案怎么算"那一列，就是机器独立重算用的方法——**和被测系统走的是不同代码路径**。

| ID | 来源 | 问题 | 答案怎么算（判卷基准） |
| --- | --- | --- | --- |
| ~~C1~~ | order_execution | ~~用药医嘱一共开了多少条？~~ | **暂缓**：行定义不稳（比值 1.13~1.69） |
| C2 | order_execution | 这些医嘱一共涉及多少个**不同的日期**？ | 抽所有日期格 → 归一化成 YYYY-MM-DD → 去重计数（**格级，不依赖行**） |
| C3 | order_execution | 医嘱里有多少个带时间戳的记录标记了静脉给药？ | 数同时含"静脉"和时间的行 |
| C4 | order_execution | 最早和最晚的**日期**相隔多少天？ | max(日期) − min(日期)（格级） |
| C5 | order_execution | 哪一天的医嘱**记录数**最多？那天有几条？ | 按日期分组数日期格 → 取最大（格级） |
| C6 | order_execution | 有几天的时间戳个数在 10 个以上？ | 按日期分组数日期格 → 数 ≥10 的天数 |
| C7 | order_execution | 凌晨 00:00 到 06:00 之间的**时间格**有多少个？ | 抽时刻格 → 落在区间内计数 |
| C8 | order_execution | 相邻两个时间戳之间，间隔最长的小时数？ | 时刻排序 → 相邻差最大（小时） |
| C9 | laboratory_report | 这个病例的检验一共抽了**多少行**？ | 值列几何聚类出的行数（**该表行定义已验证**） |
| C10 | laboratory_report | 检验里出现了多少个不同的检验项目？ | 项目名列去重计数 |
| C11 | laboratory_report | 白细胞这个项目一共测了几次？ | 项目名匹配 → 计数 |
| C12 | laboratory_report | 白细胞的测量值里，有多少次大于 10？ | 数值比较 → 计数（阈值由题面给出） |
| C13 | laboratory_report | 检验一共跨了几个不同的采集日期？ | 页脚采集时间 → 归一化到日期 → 去重计数 |
| C14 | laboratory_report | 相邻两次检验相隔最长的小时数？ | 采集时刻排序 → 相邻差最大 |
| C15 | laboratory_report | 带小数点的检验值有多少个？ | 值文本含小数点 → 计数 |
| C16 | laboratory_report | 检验值里最大的数是多少？ | 数值型值取最大 |
| C17 | 全病例 | 这个病例一共有几份文档？ | 数目录 |
| C18 | 全病例 | 这个病例涉及多少种文档类型？ | 按 category 去重计数 |

> 每病例从上面挑 3 个模板，保证 12 个病例合计 36 题，且**算一下型尽量落在 `laboratory_report`**
> （那张表的行定义已经验证过）；`order_execution` 只出格级问题。

## 四、查一下型（每病例 1 题，共 12 题）

| ID | 来源 | 问题 | 判卷基准 |
| --- | --- | --- | --- |
| L1 | laboratory_report | 最后一次检验的采集时间是什么？ | 页脚采集时间，取最晚 |
| L2 | laboratory_report | 第一次检验的采集日期是哪天？ | 页脚采集时间，取最早 |
| L3 | laboratory_report | 某个指定项目的最大值是多少？ | 该项目数值最大 |
| L4 | order_execution | 最早一条医嘱的日期是哪天？ | 日期最小值 |
| L5 | order_execution | 最晚一条医嘱的日期是哪天？ | 日期最大值 |
| L6 | medical_record_front_page | 入院日期是哪天？ | 首页日期字段 |
| L7 | medical_record_front_page | 出院日期是哪天？ | 首页日期字段 |
| L8 | ecg_report | 一共做了几次心电图检查？ | 文档数 |

> 查一下型的答案**必须逐字出现在它引用的行里**——所以判卷是"字符串／数值匹配 + 引用解析"，
> 机器全自动，人工只抽查。

## 五、库里真没有型（每病例 1 题，共 12 题）

**这一型是整份题库里最值钱的**，因为它测的是"该说没有时说了没有"。

| ID | 问题 | 素材够不够（12 病例实测） |
| --- | --- | --- |
| A1 | 这个病例有没有**输血**记录？ | ✅ **11/12 个病例可**（`transfusion_record` 文档数为 0） |
| A2 | 这个病例有没有做过**手术**？ | ✅ **3/12 个病例可**（CASE-06 / CASE-09 / CASE-10） |
| A3 | 这个病例有没有**影像检查报告**？ | ✅ **7/12 个病例可** |
| A4 | 这个病例有没有测过**某个指定项目**？ | ✅ 按项目名穷搜，命中 0 才用 |
| A5 | ~~有没有药物过敏记录？~~ | ❌ **不可用**：实测每个病例都有 5~23 处"过敏"命中 |

> **每个病例只出 1 题**，优先顺序 A1 → A3 → A2 → A4。12 个病例都凑得出来。
> 判卷：正确答案 = 「没有记录」；**答出一个不存在的记录 = 最严重错误**（错误的肯定结论）。

---

## 六、谁来核答案：你核，不用医生

| 题型 | 机器能做什么 | 你要做什么 |
| --- | --- | --- |
| C 算一下 | **独立重算**（脚本走另一套代码路径） | 抽 **6 题**（每型 2 题）看一眼 |
| L 查一下 | 匹配答案 + 验证引用可解析 | 抽 **4 题**看一眼 |
| A 真没有 | **穷搜全病例**，报告命中数 | **12 题全看**——"没搜到"不等于"没有"，这一型必须人确认 |

**你实际要核的是 22 题左右，不是 60 题。**

**工作方式**：我产出一张核对表，每道题给你：**问题 / 我的答案 / 出处（哪份文档第几行）/ 机器重算结果**。
你只需要确认"出处里写的就是这个答案"——**不需要翻整份病历**。

---

## 七、答案怎么生产（防循环）

1. **机器推导**：用几何层（值列聚类）与规则解析，**不调用任何模型**
2. **出处必带**：每个答案附 `文档:L行号`
3. **不同代码路径**：算一下型的重算脚本和建库脚本不是同一份代码
4. **你抽查**：按上表核 22 题，报"你核过的题里与机器一致的比例"

**注意**：如果某个病例的几何层解析失败（大病例），**这道题就标记"不可判"，不计入分母**——
而不是让它变成 0 分。

---

## 八、下一步

| 步 | 事 | 成本 |
| --- | --- | --- |
| 1 | 写实例化脚本：按病例挑题 + 机器推导答案 + 输出出处 | $0 |
| 2 | 跑一遍，产出核对表（22 题待人核） | $0 |
| 3 | 你核 22 题 | 你半小时 |
| 4 | 建 v1 与 v5 两版库 | 几美元 |
| 5 | 找没参与建库的模型当消费者，两版各跑一遍 | 几美元 |

**卡点**：第 4 步的 v5（机器可读说明书）还没写；v3（单位归一化）规则还没定。
这两样我可以按需现做，但建议**先跑第 1–3 步**——先把题和答案钉死，再谈库。

---

## 九、运行结果（2026-09-17，第 1–2 步已完成）

**60 题全部建成，0 题不可判。**

| 项 | 结果 |
| --- | --- |
| 题目 | 算一下 36 + 查一下 12 + 真没有 12 = **60** |
| 不可判（答案为 None） | **0** |
| 有检验文档的病例 | **12 / 12** |
| 找得到"零命中"关键词的病例 | **12 / 12** |
| 几何层数出的检验行合计 | **2518**（与结构真值一致 ✓） |

每个病例的答案摘要（日期类已遮蔽）：

| 病例 | 检验行数 | 采集日期数 | 医嘱日期数 |
| --- | --- | --- | --- |
| CASE-01 | 91 | 1 | 5 |
| CASE-02 | 290 | 3 | 10 |
| CASE-03 | 200 | 1 | 6 |
| CASE-04 | 151 | 2 | 6 |
| CASE-05 | 139 | **2** | 12 |
| CASE-06 | 330 | 5 | 12 |
| CASE-09 | 293 | 5 | 10 |
| CASE-10 | 80 | 2 | 5 |
| CASE-100 | 514 | 9 | 22 |
| CASE-101 | 144 | 2 | 10 |
| CASE-102 | 160 | 4 | 9 |
| CASE-103 | 126 | 2 | 9 |

**一个独立验证点**：CASE-05 的采集日期数是 **2**，与历史上一批实验独立得到的"该病例两次采血"一致
（那批实验做在另一个目录上，所以只能算旁证，不能算复现）。

### 建库过程中抓到并修掉的两个问题

| # | 问题 | 性质 | 处置 |
| --- | --- | --- | --- |
| 1 | `order_execution` 的"一条医嘱"两种定义差 13~69% | **设计问题**（真值定义不稳） | 该类题改为**格级**（数日期格/时间格），不出行级题 |
| 2 | 页脚四个时间标签挤在同一行，按"行带"配对会把四个格子并成一个，标签被最后一个覆盖 | **我的代码 bug** | 改为**从格子自身文本读标签**（实测标签和值本来就在同一格） |

问题 2 修掉之后，CASE-05 的采集日期数从错的 0/1 恢复为正确的 **2**，且标签分布自洽
（采集/接收/报告/打印 四类各约 6~7 个）。

### 分发方式（脱敏）

| 类型 | 题数 | 能不能带到本地 |
| --- | --- | --- |
| 算一下 | 36 | ✅ 答案是计数 |
| 真没有 | 12 | ✅ 答案是"没有记录" + 我们自己的关键词 |
| 查一下 | 12 | ⚠️ 答案是时间戳 → **在服务器端遮蔽后**才带出；核对需在服务器侧进行 |

**完整工作表（含出处与原文时间戳）留在服务器 `/tmp/qbank/worksheet.json`，不进仓库。**
本地只有遮蔽版核对表。

---

## 十、核对界面（可视化，数据不落本地）

看 JSON/Markdown 不叫核对——核对必须能**同时看到问题、答案、和引用的原文行**。
所以页面在受控主机上生成、经 SSH 隧道直接送到浏览器：

```
# 1) 受控主机上生成页面（并把证据行解析成原文）
python3 /tmp/qbank/viewer_gen.py          # 读 worksheet.json -> 写 viewer.html

# 2) 受控主机上起一个只绑本机的静态服务
cd /tmp/qbank && setsid nohup python3 -m http.server 8899 --bind 127.0.0.1 &

# 3) 本地开隧道，然后浏览器打开
ssh -N -L 8899:127.0.0.1:8899 <host>
# 浏览器打开 http://127.0.0.1:8899/viewer.html
```

**这样数据只在 SSH 通道里流动，不落本地磁盘、不进仓库。**
页面 v2：**左边是原始 PDF 页面**（用 `pdftoppm` 从符号链接的 `original.pdf` 渲染，90 DPI），**被算进去的格子用红框标出并带行号**；右边是完整清单（不截断）。每题三个按钮（对得上 / 有问题 / 跳过），
核对进度存在浏览器本地，点"导出核对结果"可复制一段 JSON 回传。

### 踩到的坑（写给以后）

**远程脚本里有中文时，不要用 `Get-Content -Raw` 读文件再 base64。**
本机 PowerShell 会用本地代码页解码，把中文读成乱码，base64 传过去的自然也是乱码
（实测：本地文件字节是对的 `e7 ae 97`＝"算"，但 `Get-Content` 读出来已经变成别的字）。

**正确做法**：直接读字节再编码——

```powershell
$b64 = [Convert]::ToBase64String([System.IO.File]::ReadAllBytes($path))
ssh <host> "printf '%s' '$b64' | base64 -d > /tmp/x.py && python3 /tmp/x.py"
```

用 `Get-Content` 时同样别写 `ssh ... "python3 -c \"...\""` 这种嵌套引号，会被 PowerShell 解析器吃掉。
**纯 ASCII 的脚本仍然首选直接管道 stdin。**

### 界面必须做到的事

**核对界面必须能同时看到"原文长什么样"和"我把哪些格子算进去了"。**
所以页面是：原 PDF 页面 + 红框叠加 + 行号，人一眼就能判断框错没框错、有没有漏页。

渲染成本：一次 359 张页面图、15.4 MB，缓存在服务器 `/tmp/qbank/img/`，跨题复用。

### 已发现、尚未修的已知问题

OCR 把日期和时刻黏在一起并用全角冒号，例如 `采集时间：2023-09-0606：43:30`。
日期能正常解析，**时分秒会丢**。影响"最后一次检验时间"这类查一下题的后半段，
**不影响算一下型的日期计数**（只用日期）。页面如实展示原文，便于人工识别。
---

# v2 重出（2026-09-17，按"砍"的要求）

## 一条设计原则（比砍哪些更重要）

> **题目的答案只能依赖"格级"和"文档级"操作，不能依赖"行级/列级"结构。**

**为什么**：日期就是日期格、文档就是文档——这两样是稳的。
而行和列正是**页码、定性结果**污染的地方（这两条都是人工核对时发现的真实问题）。

**这条原则的副作用是好的**：答案重新变成机械可靠，**不需要模型来出** → 循环论证问题跟着消失。

## 砍掉的（连同原因）

| 砍掉 | 原因 |
| --- | --- |
| 检验一共多少行 | 依赖"行"的定义；页码/定性结果都污染 |
| 带小数点的值有几个 | 同上 |
| 检验值里最大的数 | 依赖值列识别，而值列识别在血清学报告上整列失效（阴性结果被判为非值列） |
| 需要定位具体值格的查一下题 | 同上 |
| **医嘱日期跨度（天）** | **对单个坏日期太敏感**：CASE-10 实测算出 815 天，明显是坏数据 → 这类统计量不能用 |

## 保留 / 新增的模板（全部格级或文档级）

| ID | 问题 | 怎么算 |
| --- | --- | --- |
| R1 | 检验报告一共涉及多少个不同的**采集日期**？ | 带"采集"标签的日期格 → 去重计数 |
| R2 | 用药医嘱一共涉及多少个不同的**日期**？ | 医嘱文档的日期格 → 去重计数 |
| R4 | 这个病例一共有**几份检验报告**？ | 数文件 |
| R5 | 所有文档里一共出现多少个不同的日期？ | 全病例日期格 → 去重计数 |
| L1 | 最晚的一条用药医嘱是**哪一天**？ | 医嘱日期格取最大 |
| L2 | 最早的一次检验采集是**哪一天**？ | 采集日期格取最小 |
| A1 | 有没有做过"胃镜"？ | 全病例穷搜，命中 0 → 答案"没有记录" |

**每病例 3 算一下（从 R1/R2/R4/R5 里按可用性取前 3）+ 1 查一下 + 1 真没有 = 5 题 × 12 病例 = 60 题。**

## 新的可用性规则

**证据基础为空就不出这道题**（例如某个病例没有带"采集"标签的日期格，就不问 R1），
改取下一个可用模板。**实测：12 个病例 × 3 题，全部有非空证据基础，0 题不可判。**

| 病例 | 采集日期数 | 医嘱日期数 | 全病例日期数 | 检验报告数 |
| --- | --- | --- | --- | --- |
| CASE-01 | 1 | 5 | 8 | 1 |
| CASE-02 | 3 | 10 | 13 | 1 |
| CASE-03 | 1 | 6 | 15 | 1 |
| CASE-04 | 2 | 6 | 11 | 1 |
| CASE-05 | 2 | 12 | 16 | 1 |
| CASE-06 | 5 | 12 | 28 | 2 |
| CASE-09 | 5 | 10 | 11 | 2 |
| CASE-10 | **0**（无带标签的采集日期格） | 5 | 10 | 1 |
| CASE-100 | 9 | 22 | 26 | 1 |
| CASE-101 | 2 | 10 | 11 | 1 |
| CASE-102 | 4 | 9 | 17 | 1 |
| CASE-103 | 2 | 9 | 13 | 1 |

## 人工核对时发现的四个 AI-Ready 缺口（保留为论文素材）

| # | 发现 | 性质 | 对应记分卡的哪一维 |
| --- | --- | --- | --- |
| 2 | **页码/时间被当成检验值**（框在"第 1 页 共 1 页"上） | 精确率错 | 值列识别必须排除页脚带 |
| 3 | **阴性结果整列漏抽**（血清学报告 8 行只框到 4 个） | **召回率错，且是语义类** | 库里必须能放**定性结果**，不能只认纯数字 |
| 4 | **散点图没进库**（WDF/WNR/RBC/PLT） | 多模态内容缺失 | 页面/文档需要"含图像"字段 |

**第 3 条最要紧**：值列判定要求"六成以上是纯数字"，而血清学报告的"结果"列写的是 `阴性(-) 0.40`——
整列被判为非值列，**系统性漏抽**。这是"AI-Ready 缺什么"最硬的一条实证。