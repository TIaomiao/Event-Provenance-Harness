# 可复用的医疗数据准备 Harness + Agent：现役研究规格

本文件是**唯一现役研究规格**。当前状态与已定选择见 [CONTROL_PANEL.md](../CONTROL_PANEL.md)；文献判断见 [related_work/AI_READY_EVIDENCE_MATRIX.md](../related_work/AI_READY_EVIDENCE_MATRIX.md)。

## 论文概览

> **本节是当前工作稿：我们准备做什么、打算怎么验证。它是假设与方案，不是已验证结论。**

**工作题目**：可复用的医疗数据准备 Harness + Agent

**一句话**：把扫描病历中的处理能力组织成可调用、可核验、可复用的系统，并验证它对后续 AI 使用任务的帮助。

英文工作题目（未定稿）：*A Reusable Data-Preparation Harness for Real Hospital Records: Does It Make Off-the-Shelf Agents More Useful?*

**Motivation**

1. **真实扫描病历这一段没人量过。** 现有「AI-ready」工作要么是人工等级量表与准备指南（JCM 2026 的 0–2 级量表，作者自陈是提案而非经验证工具），要么比的是「已经结构化之后怎么排版」（FHIRBench 用合成 FHIR bundle）。**从扫描件到可用数据中间那一段，没有人量过每一步各值多少。**
2. **「准备之后谁在用、怎么测」也还没回答。** Infherno 能把自由文本变成 FHIR，但止步于合成质量；FHIR-AgentBench 把「取对记录」与「答对问题」分开计分，但金标准只到资源 ID 级。**「输出了结构」与「下游更好用」之间的连线没有被建立。**
3. **规模上可能不是「更贵」，而是「能不能做」。** 队列级问题才是临床真正要的；在单次输入的限制下，原文的可答规模存在硬上限。

**研究问题**

> 把准备能力组织成可调用的 Harness、由 Agent 编排调用，能否让后续 AI 在真实的整理与核验任务上更可靠——**与「原文 + 合理访问」和「可复现的常规准备流水线」相比，好在哪里、好在多少、代价是什么？**

三个子问题：① 哪些准备动作各自贡献多少，哪些没有贡献？② 这种贡献是否**跨模型、跨题目、跨病例**仍然成立（而不是某个模型的偏好）？③ 代价（token、调用、建库、人工核对）与收益的关系是什么形状？

**待验证主张**（全部待检验，不预设正结果）

| 编号 | 主张 | 怎样会被推翻 |
| --- | --- | --- |
| H1（分任务类型） | 把字段含义、状态码与计数规则写成机器说明书，能提高下游 AI 在**计数与阈值类**问题上的表现；对**单文档查找与极值类**问题没有增益 | 换模型 / 换题目后差异消失，或差异只来自输入 token 数 |
| H2（分任务类型） | 把时间排成纵向轴并按天聚合，能提高**时间类**问题的表现 | 同上；当前幅度区间很宽，只能报区间 |
| H3（问法，不是负结论） | 来源与状态字段对 ①答题正确性 ②证据核验效率 ③错误识别分别有什么作用？ | ——这是待答问题；已有的单题集观察不足以写成「无价值」 |
| H4（限定条件） | 在**单次输入、指定模型与配置**下，不做准备时聚合类问题在规模上不可行；该上限在允许分块 / 检索后**尚未测** | 允许分块、检索、逐步读取后该上限消失 |
| H5 | 准备的收益可能体现在**成本与运行失败率**上，而不只在答对率 | 在预定正确性容差内，成本与失败率也都不改善 |

**候选贡献**（候选，不是已成立）

1. **一个可复用的准备 Harness**：把版面读成行、单位归一、来源与状态、时间关联、机器说明书组织成可调用、可核验的接口。复用的含义必须可检验——跨病例、跨未见任务复用同一套组件，减少逐题定制。
2. **一套对照评测设计**：在候选任务上比较「完整方案」与两条对照（原文 + 合理访问、可复现的常规准备流水线），同时报质量、成本与失败，给出质量—成本曲线。
3. **一份可复现的答案依据与判定协议**：三层答案键（机械 / 独立模型 / 人工核对）+ 独立于被测库的程序对照键；机械问题与需要临床解释的问题分开。

**本节要一起读的限定**：多层级处理是**候选消融**，不预设每层正收益，层数不是贡献；主题是「整体方案 + 可复用性」，不是「逐层涨分」。目标架构、代码存在、接口可调用与端到端验证是四件不同的事。

## 一、问题

真实医院的病历是**扫描 PDF**，不是数据库。要让它们成为「现成的 AI 能直接用」的东西，中间必须做一系列准备动作（把版面读成行、统一单位、标上来源、排好时间、写清字段含义……）。问题分两层：

1. **差异层**：AI-ready 与常规做法（不做准备，或按治理规范做常规准备）的**本质区别**是什么？好处落在哪个维度上？
2. **设计层**：把准备工作组织成可调用、可核验、可复用的系统（Harness），由 Agent 编排调用，能不能在真实任务上带来可测量的改善？

**不预设准备动作总带来正收益。** 现状是：AI-ready 更多是标签或人工等级量表；现有的对照实验多在「已经结构化」的输入上做。我们检索范围内未见同形工作，这是**检索覆盖内的待核缺口**，不是「无人研究」的证明。

待回答的七个问题（会议 §5.3）：

| # | 问题 | 现在能答到什么程度 |
| --- | --- | --- |
| 1 | AI-ready 的操作性定义是什么？面向哪种使用者与任务？ | 见「论文概览」的研究问题与候选贡献；面向科研资料整理与核验 |
| 2 | 与何种「非 AI-ready」或常规方案比较才公平？ | 已有两条对照默认稿（§七），未实测 |
| 3 | 核心优势是任务完成、少做准备、成本、稳定性，还是核对便利？ | 见「论文概览」的 H1–H5；每条主张各有独立定义与证据 |
| 4 | 什么证据能判定任务完成或结论正确？ | 已有三层答案依据默认稿（§八），未运行 |
| 5 | Harness 与 Agent 分别承担什么？本文研究准备、使用方式，还是完整系统？ | 按 §四 分角色；贡献范围已由用户选定为「可复用的医疗数据准备 Harness + Agent 整体方案」 |
| 6 | 哪些医疗场景特性需要适配，而非通用清洗的复述？ | 真实扫描件、多版式、纵向时间、定性结果、合规去标识 |
| 7 | 哪些规模、模型和任务足够验证主张？ | 未定；待主表定稿后定 |

## 二、目标使用者与使用场景

- **直接使用者**：需要从真实病历里取事实与统计的**研究者**（临床研究者与数据整理者），不是终端患者，也不是实时临床决策者。
- **初步场景**：**科研资料整理与核验**——给定一份或一批病历，取回一个事实、一张表或一个计数，并给出依据与未决状态。
- **候选任务**（待任务设计与师兄反馈；**不能写成已临床验证的需求**）：
  - **T1 单病例事实与时间整理**：某项目的极值、超阈值次数、某事件的最早/最近日期。
  - **T2 跨病例筛选与统计**：一批病例里有多少例满足某个条件。
- **下游消费者**：没参与建库的现成 AI / Agent，一次调用或多步调用（方式待定）。

## 三、输入与输出

| 环节 | 定义 | 现状 |
| --- | --- | --- |
| **输入** | 真实医院扫描 PDF（受控环境切片，已获数据许可） | 已有可用批次 |
| **准备产物** | 可复用的数据产物与访问接口：原文、结构化观测、单位归一、溯源与状态、机器说明书、纵向时间轴 | 已有实现，作候选组件 |
| **输出** | 答案 / 表格 + 依据（引用可解析）+ 未决状态 | 接口已实现 |
| **运行状态** | 接口成功、内容正确、适当拒答、运行失败、成本，各自独立定义 | 口径见 §八 |

## 四、整体架构

**目标结构，不等于已经端到端实现。**

```text
科研资料整理 / 核验任务
          ↓
Agent：理解任务、选择步骤、在预算内编排
          ↓ 调用
Harness：文档读取与定位、提取与规范化、时间与来源关联、
         检索与分页读取、程序计算、格式与内容核验等可调用能力
          ↓
可复用数据产物与访问接口
          ↓
后续任务使用 → 答案 / 表格 + 依据 + 未决状态

旁路：评测器检查任务是否通过，并独立记录资源代价与失败。
```

**角色分开，但不要求建立多个 Agent**（可以复用同一基础模型或同一进程）：

- **准备流程 Agent**：把原始文档变成可复用产物。
- **下游任务消费者**：使用产物完成任务并给出依据。
- **自动实验驱动器**：按协议跑条件、做自检、产出只含聚合数字的结果。

**复用的含义必须可检验**：跨病例、跨未见任务复用同一套组件，减少逐题定制；**准备阶段不得读取留出题目或其答案**。

## 五、组件与现有实现对照

「代码存在」「离线测试通过」「有历史运行」「新协议端到端验证」是四件不同的事，下表分别标注。本轮未运行任何真实数据实验。

| 功能 | 实际路径 / 受控端位置引用 | 输入 → 输出 | 当前证据（日期） | 与当前主线的关系 | 待验证点 |
| --- | --- | --- | --- | --- | --- |
| 时间角色与观测绑定 | `src/prototype/temporal_roles.py` | 候选坐标 + 行链接 → 绑定结果 | 离线合成单测（2026-09-19 实跑，`src/prototype` 整包 37/37 OK） | 候选组件：时间与来源关联 | 单候选只允许一个行链接，不支持表头共享日期；`api_ready=false` 保留，未改 |
| 时间证据核对与共同坐标旁表 | `src/prototype/temporal_rebase.py` | 病例目录 + 生产日期源码 → 私有旁表 | 离线合成单测（同上） | 候选组件：时间归一 | 需已获准病例目录与来源哈希；扩展时受来源规则阻断保护 |
| 检验表几何适配 | `src/prototype/laboratory_bbox_adapter.py` | 版面块 → 表格行 | 离线合成单测（同上） | 候选组件：版面 → 行 | 只覆盖同页规则表；续页与跨页未支持 |
| 观测级评分器 | `src/evaluation/scorer.py` + `tests/` | 参考答案 + 提交 → 匿名聚合指标 | 离线合成单测 **45/45 OK**（2026-09-19 实跑） | 评测器旁路 | 真实参考集未建立；主指标口径待冻结 |
| 合成夹具与诊断演示 | `src/evaluation/fixtures.py`、`demo.py` | 合成观测 → 五种错误的诊断输出 | 开发材料，非证据 | 只做自检，不产生研究结论 | 不得当作模型结果 |
| 阶梯构建 / 驱动 / 出题 / 聚合 / 重打分 | 本地忽略目录 `.local/bench_ladder_*.py` | 整批文档 → 多层版本与聚合数字 | 有历史运行（2026-09-19，`results/2026-09-19_ladder.md`） | 用于说明「分层归因可跑通」的探索 | 本轮未复跑；不进公开仓库 |
| 多消费者复现 | 本地忽略目录 `.local/bench_consumers.py` | 同一批题 → 多模型结果 | 有历史运行（2026-09-19） | 探索性 | 真实返回型号未记录；家族独立性未证实 |
| 治理流水线与印章处理 | 受控端 `SCS-EHR-Governance/config/mvp.yaml` | 原始 PDF → OCR/版面产物 | 只读引用，**本轮未访问** | 数据基础，按需复用接口 | 只读复用，不改生产配置 |
| 历史抽取结果 | `results/*.md` | — | 前期预实验证据 | 不进主结果位置 | 见 §十二 |

## 六、候选任务（输入输出草案）

| 任务 | 输入 | 请求 | 输出 | 判定依据（草案） | 现状 |
| --- | --- | --- | --- | --- | --- |
| **T1** 单病例事实与时间整理 | 单病例全部文档（按条件给原文或准备产物） | 某项目的极值 / 超阈值次数 / 某事件最早日期等 | 值或日期 + 引用 + 未决状态 | L1 机械键（几何层零模型产出）+ 独立模型复核 | `NOT_RUN` |
| **T2** 跨病例筛选与统计 | 一批病例 | 满足条件的病例计数 | 计数 + 口径 + 依据 | 独立于被测库的程序 / SQL 对照键 | `NOT_RUN` |

## 七、方法与对照

**两条对照并行，不是三选一。**

| 对照 | 它代表什么现实做法 | 必须包含的能力 | 已知风险 |
| --- | --- | --- | --- |
| **B1 原文 + 合理访问** | 不做准备，但允许正常使用工具 | **分块、检索、逐步读取、通用计算**；一次性输入只作**诊断条件** | 若不给这些能力，规模一大就不可比 |
| **B2 可复现的常规准备流水线** | 按常识整理一遍 | 不等于「只加格式」；不故意削掉它的合理能力 | 边界模糊，容易被质疑是稻草人 |
| 少量独立原文核对 | 全人工整理的代表 | 抽样核对，不作为必做的大规模基线 | 人工核对不是自动无误的金标准 |

**主方案（整体方案）也允许分块、检索、逐步读取与通用计算**；若加入专属索引 / SQL 工具，必须分开报告「整体系统效果」与「表示 / 工具」两个因素。

## 八、指标、答案依据与成本

**主指标候选**（草案）：**给定预算内、通过预定核验的任务比例**。正确性与接口完成分开；成本、人工介入、核对时间各自独立成维度，**不合成任意总分**。正式验证前冻结主指标与容差。

| 维度 | 定义 | 失败怎样记录 |
| --- | --- | --- |
| 接口成功 | 请求返回且符合输出接口 | **接口/工具失败**与**格式失败**分别记 |
| 内容正确 | 与答案依据在预定容差内一致 | **答错**与**错否定**分别记 |
| 适当拒答 | 依据不足时说明不知道 | 该说不知道却编 → 记为错答；不该拒答而拒答 → 记为覆盖缺口 |
| 运行失败 | 预算耗尽、超上下文、进程错误 | 与答错分开单列 |
| 成本 | `C_total(N) = C_prepare + C_index + Σ(C_query + C_retry/repair)` | 人工时间另报；未知成本标「未知」 |
| 核对便利 | 引用可解析率 + 走到原文的步数（**代理指标**） | 真人计时的「医生核对时间」未做 |

**答案依据三层，分开报告**：L1 机械层（格子几何，零模型产出）／ L2 模型层（比被测更强的模型出候选，记录版本）／ L3 人工层（抽样核对）。**造键的模型必须 ≠ 被测模型，并留一个没参与造键的模型做留出评测。** 另设**独立于被测库**的程序 / SQL 对照键。

## 九、主张映射表与主结果表（两者分开）

两张表职责不同，**不合并、也不互相替代**。

**表 1 主张映射表**：一行 = 一项可检验主张，回答「这条主张怎么算成立、怎么算失败、不能据此说什么」。每条主张各有独立定义与证据。

| 核心主张 | 对照与实验条件 | 固定项 | 变化项 | 主指标 | 判定依据 | 失败怎样记录 | 不能据此声称什么 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **H1** 说明书对计数与阈值类问题的增益 | 完整方案 vs B1 / B2；按任务类型分层报 | 同一批病例与题目、同一消费者、相同预算上限 | 准备产物是否含机器说明书 | 通过预定核验的任务比例（分层报） | L1 机械键 + 独立模型复核 | 答错 / 格式失败 / 接口失败 / 预算耗尽分别记 | 不能外推到未测的任务类型 |
| **H2** 纵向时间轴对时间类问题的增益 | 同上；只看时间类题目 | 同上 | 是否含纵向时间轴 | 同上 | 同上 | 同上 | 幅度区间宽，不能报单点提升 |
| **H3** 来源与状态字段的作用 | 分维度：正确性 / 核验效率 / 错误识别 | 同上 | 是否含来源与状态字段 | 正确性 + 引用可解析率 + 走到原文步数 | 独立于被测库的程序键 + 引用抽查 | 核验效率只有代理指标，未做真人计时 | 不能写成「该字段无价值」 |
| **H4** 规模上限与对照条件 | 单次输入（诊断条件）vs 允许分块 / 检索（正式对照） | 同一批病例、同一问题集 | 是否允许分块、检索、逐步读取 | 可答规模 + 容差内正确率 | 程序统计键 | 超上下文记**运行失败**，不记答错 | 不得写成「原文物理上做不了」 |
| **H5** 成本与失败率 | 完整方案 vs B1 / B2 | 相同预算上限 | 准备与索引成本是否计入 | `C_total(N)` + 运行失败率 | 调用与 token 账本 | 未知成本标「未知」，不填 0 | 不能只报有利的那一段成本 |

每条主张自己定义**正向 / 负向 / 预先定义的实际等价 / 证据不足**四态；**「未测出显著差异」不等于「已证明等价」**。

**表 2 主结果表**：一行 = 一个方法或条件，列 = 结果与资源代价。**结果表不要求「一行一个主张」**——同一个条件可能既更准又更便宜，四种运行状态与多项指标各自成列；主张与证据的对应关系由表 1 负责。

| 条件 | 通过预定核验的任务比例 | 正确 | 错误 | 适当拒答/信息不足 | 预算耗尽 | 格式失败 | 接口/工具失败 | 未运行 | 输入 token | 输出 token | 调用次数 | 重试/修复次数 | 建库成本 | 索引成本 | 人工分钟 | 延迟 p50/p95 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **B1** 原文 + 合理访问（分块 / 检索） | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN |
| **B2** 可复现的常规准备流水线 | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN |
| **完整方案**（Harness + Agent） | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN |
| **候选消融**（逐层加 / 逐层拿） | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN | NOT_RUN |

**填写规则**：未运行一律写 `NOT_RUN`，不得把未运行冒充结果；键、题目、分母与筛选规则定稿后如有改动，必须记录并重跑全部条件。

## 十、公平性

| 问题 | 默认工作稿 |
| --- | --- |
| 原文那一方允不允许分块 / 检索 / 逐步读取 | **允许**；一次性输入作诊断条件 |
| 准备后那一方允不允许查询 / 计算工具 | 允许**通用**计算；专属 SQL / 索引须分开报告 |
| 双方预算怎么匹配 | 相同**预设资源上限**下比较，报告**实际消耗**；不强迫各方花完同样 token |
| 建库成本计不计入 | **计入**；摊销次数**预先规定**，不挑对本方法有利的 N |
| 病例数与题目数 | 待定；**留出未参与开发的病例与题目**做验证 |
| 消费者模型数量与家族 | **≥2 个家族**；记录请求别名 / 返回型号 / 提供方 / 版本 / 哈希 |
| 答案依据 | 机械查询统计 + **独立于被测库**的程序 / SQL 对照键 |
| 脱敏与授权 | **所有条件的共同前提**，不为制造分数差而关闭 |

## 十一、候选消融：多层级处理

已有实现是一条**严格超集**链：生数据 → 格式统一 → 单位归一 → 溯源与状态 → 机器说明书 → 纵向时间轴。每个版本保留上一版全部内容、只加一样东西。

**归因的限度（重要）**：超集构造消除了「换了别的东西」这一类混淆，但**不足以把差异唯一归因于单一组件**。至少还有三个未被控制的量会同时变化——**输入总长度 / 材料总量**、**预计算与物化的程度**、**提示长度**。因此：

- 超集差值只能读作「**在这一串构造下，加上这一样之后结果如何**」，不能读作「这一样单独值多少分」。
- 要支持组件级归因，至少还需要**反向拆件**（从最完整版本逐样拿走）与**长度/预算匹配的控制条件**；这些尚未做。
- 若准备材料里含与测题直接对应的预计算摘要，必须写明这是「预计算 / 物化」的成本收益，不是模型推理能力的提升。

**定位**：这是用来**选择性解释组件作用**的候选拆解，**不是**论文必须成立的结论序列，也**不预设每层正收益**；层数本身不是贡献。固定准备策略，**不得读取留出题目或其答案来定制材料**。

已有探索性结果（2026-09-19，`results/2026-09-19_ladder.md`）：批次 984 例 / 5,255 文档；病例级 12 例 × 11 题 × 9 条件 × 3 次重复，消费者与建库过程无关（建库为零模型机械脚本）。生数据 61.6% → 机器说明书 73.0% → 纵向时间轴 81.9%，前四层单独看几乎不动分。**这些数字是探索性结果**，只用来支撑「分层归因这个做法可跑通」，并带着上面那条归因限度一起读。

## 十二、已有素材的定位

| 素材 | 定位 |
| --- | --- |
| 旧抽取 benchmark（引用 / 值 / 时间接地那套） | **前期预实验**，不进主结果 |
| 六层超集曲线（2026-09-19） | **探索性结果** |
| 队列级容量实验 | **探索性结果**；要与 HoloBench 的既有观察一起写 |
| 答案键三层协议 | **可复用资产**，进主表「判定依据」列 |
| 60 题试跑、难题集 | 历史探索结果 |

## 十三、当前局限

- **无医生金标准**：内容正确率与临床等价匹配做不了；指标全部落在「机械可判 + 可核对」这一层。
- **单人核对答案键**：报不出标注者间一致性。
- **消费者模型**：已有三家命名均为 Gemini 系，**家族独立性未证实**，真实返回型号 / 提供方 / 版本未记录；因此不得声称模型无关。
- **主表未填**：主张、对照、指标、判定依据仍是草案，成绩一律 `NOT_RUN`。
- **两条对照未实测**：B1 的分块 / 检索、无检索基线、输出预算扫描都还没做。
- **队列级结论有适用条件**：限定在「指定模型、指定配置、单次输入」；分块、检索、逐步访问、索引与计算工具**均未被排除**，不得写成「原文物理上做不了」。
- **组件未联通**：现有原型是局部模块，不是完整的 Harness + Agent；接口可调用不等于端到端可用。
- **本地未核**：受控端服务器运行目录、计划任务与服务状态本轮未访问。
- **影像模态不做**：语料是扫描 PDF，没有 DICOM、没有影像标注；影像只做报告文本题、含图页指标与字段预留。

## 十四、待上级判断（不自行裁定）

1. **医疗任务的专业含义与合格参考标准**：哪部分必须由临床专家裁定（例如治疗起点的定义），开发侧不得自行拍定。
2. **超出现有授权的动作**：数据访问扩展、对外发送、新增付费调用预算、额外人工标注、计算资源、投稿承诺。
